name: scopo
description: "A Flutter package for managing scopes and dependency injection within the widget tree"
version: 0.9.0
homepage: https://github.com/vi-k/scopo

environment:
  sdk: ^3.2.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  logger_builder: ^0.3.0
  meta: ^1.16.0

dev_dependencies:
  ansi_escape_codes: ^3.0.2
  fake_async: ^1.3.2
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  test: ^1.24.0

# Бэклог

> Записи владельца: что хочется сделать. Пишет владелец, агент читает,
> предлагает взять пункт в работу и удаляет выполненное (правила — в
> `AGENTS.md`, раздел 3). Состояние проекта — в `docs/handoff.md`.

- **Семья, где стрим кормит модель после готовности.** Отправлено сюда
  2026-09-22 из открытых вопросов `docs/handoff.md`. Сегодня такого нет
  ни у кого: `StreamSubscription` в `lib/` не встречается вовсе, а `each`
  ядра это не закрывает по построению — он член `JobContext`, то есть живёт
  внутри инициализации, и подписка «на всю жизнь скоупа» через него держит
  скоуп в вечной ветке загрузки (замер — в «Что дальше» `docs/handoff.md`).
  Вместо семьи написан рецепт из двух готовых: раздел «Following what the
  controller hears» в `doc/async_controller_scope.md` и три теста
  в `test/controller_notifier_recipe_test.dart`, отчёт —
  `docs/records/2026-09-22[8]-controller-stream-recipe-report.md`. Довод
  за семью придёт оттуда же: в каждом таком скоупе руками пишут обёртку
  `ScopeNotifier.value` в `buildOnReady` и аксессор-статик на контроллере —
  две вещи на семейство. Поверхность новая, поэтому начинать с записи дизайна
  и слова владельца.

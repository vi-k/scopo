# Состояние проекта

> Обновлено: 2026-09-09. Файл поддерживается агентом и переписывается по ходу
> работы — это снимок «здесь и сейчас», а не история и не список пожеланий
> (он в `docs/backlog.md`). История — в `docs/records/`; историческая часть
> самого этого файла вынесена 2026-08-30 в
> `docs/records/2026-08-30[1]-handoff-history-log.md`.

## С чего начать после рестарта

1. Прочитай этот файл целиком, потом `docs/backlog.md`.
2. Сверься: `git log --oneline -5`, `git status`.
3. **Кода в работе нет, открытых решений нет, очередь ревью пуста.**
   Все списки исчерпаны, у каждой находки вердикт; дерево чистое, `main` и
   `origin/main` совпадают, гейт §6 и машина зелёные на голове. Своей работы,
   которую надо подхватить прямо сейчас, тут нет — **не выдумывай её**: спроси
   владельца, что дальше, или предложи пункт из `docs/backlog.md`, он сейчас
   пуст.

   **0.14.0 опубликована, протегирована и запушена** — на pub.dev 2026-09-09
   по явной просьбе владельца, гейт §6 перед этим пройден целиком (числа — в
   «Проверках»), тег `v0.14.0` на `b0c3830`.

   **Порядок выпуска теперь в регламенте** — `AGENTS.md` §5, подраздел «Выпуск
   версии»: связка целиком и подряд, разрешение только явное и только на неё.
   Правило дано владельцем 2026-09-09 после того, как этот выпуск прошёл
   иначе. **Прочти его перед любым следующим релизом**; механика — в
   «Порядке публикации» ниже.

   **Переводить зависимости на `async_job` не надо — этот вопрос закрыт
   2026-09-09, ответ отрицательный.** Владелец спросил, почему при переходе на
   ядро (`bc914c8`) `ScopeDependency` осталась своей. Ответ разложен в
   `docs/records/2026-09-09[7]-dependency-job-design.md`, ревью на него — в
   `docs/records/2026-09-09[8]-dependency-job-report.md` (`codex`, 11 находок,
   все приняты, вердикты на месте, зонды прогнаны), а утверждение, на котором
   стоит вывод, проверено отдельно —
   `docs/records/2026-09-09[9]-context-reach-report.md`.

   Итог: **`bc914c8` остановился ровно там, где следовало.** Ядро окупается
   там, где есть тело, разговаривающее с контекстом (инициализация скоупа), и
   там, где нужна настоящая область отмены с ветвями (параллельная группа) —
   оба места уже переведены. Ниже дерево зависимостей — обход, а не набор
   задач: во всём `full_scope/` контекст трогают в четырёх местах, у листа
   тело получает `ScopeDependencyHandle`, а `_runInit` принимает `ctx` и не
   обращается к нему ни разу; задача же на группе забрала бы себе контекст,
   который сегодня получает чужая реализация, и сломала бы ей `onDiscard` и
   ожидание.

   **Ядро при этом ни от кого не закрыто**, и это довод в ту же сторону: чужая
   реализация получает контекст по интерфейсу и может позвать `ctx.run` сама, а
   обычный лист достаёт контекст через свой контейнер — `init` у
   `ScopeAutoDependencies` виртуальный. Задача на узле не открыла бы новой
   возможности, только вставила бы лишнего владельца.
   **Не начинай эту работу заново.** Если кажется, что смысл есть, — читай
   сперва «Что уцелело: ничего» в спеке: там разобран и последний кандидат.

4. **Послеволновое ревью пройдено целиком: 22 находки, все закрыты.** Списки —
   `docs/records/2026-09-09[5]-post-wave-review.md` (мой проход, там же
   сведение обоих и вердикты) и
   `docs/records/2026-09-09[6]-post-wave-review.md` (`codex`, текст как отдан).
   У каждой из двенадцати находок M/L вердикт в конце неё; у каждого из десяти
   Info — исход («закрыто» или «оставлено намеренно», с причиной).

   Шесть коммитов правок, у каждого свой падающий тест и проверка
   нагруженности откатом; **тестов 525 → 534**, гейт §6 пройден целиком перед
   каждым мержем. Что уехало:

   - **M1** — одна ошибка дочерней задачи давала два отчёта. Адаптер помнит по
     идентичности, что уже объявил, и флаг ставится условно;
   - **M2 и M3** — бросок пользовательского `tag.toString()` или чужого
     `timeoutCallbacks` обрывал разбор до его начала: у скоупа не возвращался
     `scopeKey`, у координатора оставалось смонтированным всё, что стояло за
     ним в партии. Обе метки читаются под оградой. **Дефект скоупа выпущен в
     0.13.0**, а не привнесён волной;
   - **M4** — `walkEnded` перестал угадываться: обход сам говорит, дошёл ли он
     до всех (`_walkReachedEveryChild`), и группа сама говорит, может ли
     что-то ещё держать (`_mayStillHold`). Громкий отказ повторному `init()`
     вернулся туда, где ему место, — на чужую реализацию `ScopeDependency`;
   - **M5** — вторая ветвь `concurrent`, упавшая до отметки, объявляется
     вместо того, чтобы падать на пол в `failure ??=`;
   - **M6 и L5** — страховка укрытой ошибки уехала из элемента в адаптер:
     элемент владеет одной задачей, адаптер видит все. Заодно исчезли
     `_reportCoveredBodyFailure`, `_bodyErrorCovered` и `_bodyStackTrace`;
   - **L1** — обещание про `FlutterError` снято из темы, зеркала и
     `CHANGELOG.md` (решение владельца: текст, не код);
   - **L2** — тест позднего таймаута больше не превращает падение в
     десятиминутное зависание;
   - **L3** — отчёт пост-отменного канала называет зависимость путём всего
     дерева: у канала нет дороги вверх, поэтому путь спускается вниз
     (`_pathOf`);
   - **L4** — `Cancelled` из `dep.dispose` останавливается у наблюдателя
     (решение владельца);
   - **L6** — безымянная ветвь группы называет свою позицию вместо `Job()`.

   **Ничего не осталось открытым.** Следующий шаг — решение владельца о
   публикации 0.14.0: находок, которые её держали, больше нет.


5. **До того, в тот же день, — ревью на дефекты по всему, что легло после
   шестого ревью. Двенадцать находок; список пройден целиком.** Мой проход —
   `docs/records/2026-09-09[3]-bug-hunter-review.md`, второй, независимый, —
   `docs/records/2026-09-09[4]-bug-hunter-review.md` (`codex`). Сведение,
   сходимость и пробелы покрытия — в первом.

   **Список пройден целиком, тесты 513 → 525.** Закрыты C1, H1, M1, M2, M3,
   M4, M5, M6, L1, L2, L4 и весь хвост Info; **L3 не подтвердилась** (окна
   нет, разбор в её вердикте). Вердикт стоит у каждой
   находки, в самом файле ревью. Владелец 2026-09-09 сказал «чини всё подряд,
   пока не понадобятся мои решения» — работа шла по списку сверху вниз и
   остановилась на двух вопросах о публичной поверхности, они ниже.

   **Оба решения владельца приняты 2026-09-09 и выполнены:**

   - **вторая половина M4 — кешировать четыре колбека.** Сделано: они
     снимаются с виджета одним значением в начале разбора, рядом с меткой;
     двадцать переопределений в пяти семействах свернулись в пять геттеров
     `timeoutCallbacks`, сами хуки остались точками переопределения;
   - **`ScopeInitJob<T> extends JobBase<T>` — оставить, записать риск.**
     Записано в «Тулчейн и окружение»: ломающим это станет на апгрейде ядра
     внутри `0.1.x`, и смотреть надо тогда.

   **Что дал прогон сверх чтения.** Три находки (M4, M5, L1) были получены
   ревьюерами только чтением — все три подтвердились зондами, слово в слово.
   **L3 чтением же и не подтвердилась.** **L4 закрылась правкой M6** — это
   оказался один механизм, увиденный с двух сторон, и спор двух проходов о
   ней разрешён прогоном: правы обе стороны, каждая наполовину. И зонд по M4
   нашёл соседа, которого не видел никто, — те самые двадцать мест.

   Что было главным и что с ним сделано:

   - **C1, Critical: тело, отменившее само себя, вешает экран навсегда и
     молча.** Ядро прямо предлагает идиому `throw Cancelled('why')`, `scopo`
     реэкспортирует этот тип и учит его ловить в `README.md` и
     `doc/full_scope.md`, а `_settleInit` на ветке `case Cancelled(): break`
     молчит — он рассчитан на то, что отмену просил только
     `_prepareForDisposal`. У ядра источников отмены три. Экран остаётся на
     загрузке, наблюдатель нем, в `FlutterError` пусто. **Это регресс уже
     закрытого дефекта:** до перехода тот же исход ловил `onDone` потока и
     поднимал `StateError` со словами про «the quietest way for a scope to
     fail». Подтверждено моим зондом. **Исправлено:** разбор исхода смотрит
     на `reason`, самоотмена уходит в `_settleFailure`, отмена по просьбе
     разбора остаётся молчаливой.

   Дальше по тяжести: **H1** (ошибка тела, накрытая поздней отменой, не
   доходит ни до кого — страховка ядра `_reportCovered` для `scopo` мёртвая
   по построению, потому что он смотрит исход всегда и заранее), **M1**
   (ходка уборки, дошедшая до конца, не считается законченной, и контейнер
   навсегда отказывает в повторном `init`), **M2**–**M6**, пять Low и шесть
   Info. **H1 исправлена вместе с C1** — они жили в одном месте; M1 и всё
   остальное закрыто тем же днём, вердикты — в самих находках.

   **Три находки получены больше чем одним исполнителем независимо** — H1
   тремя, C1 и M1 двумя. Все три подтверждены зондами.

   **Мутацией найден пробел покрытия:** владение построенным значением держится
   парой защит, и каждая половина по отдельности не закреплена ничем — снятие
   любой из них поодиночке оставляет все 513 тестов зелёными. Ни C1, ни H1
   существующая сьюта тоже не держит.

   Порядок работы по находкам — обычный, `AGENTS.md` §8 и §4: сперва падающий
   тест, потом правка, потом проверка нагруженности откатом; у каждой
   проработанной находки — вердикт абзацем в конце неё, в самом файле ревью.

6. До того — **2026-09-09, `bc914c8`: переход на ядро `async_job`
   сделан целиком и смержен в `main`.** Спека —
   `docs/records/2026-09-09[1]-async-job-migration-design.md`, ход работы
   исполнителя — `docs/records/2026-09-09[2]-async-job-fixes-report.md`.
   Что уехало:

   - **собственная копия контекста задачи удалена.** `ScopeInitCancelled` и
     `ScopeInitHandle` ушли, `ScopeInitContext` наследует `JobContext` ядра и
     добавляет только `progress`, наружу инициализацию водит новый
     `ScopeInitJob<T>`; имена ядра выборочно реэкспортированы, второй
     зависимости потребителю не нужно;
   - **элемент оседает на исходе задачи**, а не ловит своё исключение;
     ошибки, у которых нет исхода, идут через адаптер наблюдателя;
   - **значение, построенное для скоупа, который уже сдался, лежит на стеке
     уборки ядра**, а поле заполняется только на ветке `Done` — прежняя
     запись в теле давала двойное освобождение;
   - **ветви параллельной группы стали дочерними задачами**: одна задача на
     все ветви превратила бы отказ ветви в отмену группы, и экран показал бы
     загрузку вместо ошибки;
   - **правило идиомы перевернулось** вслед за ядром: `wait` принимает
     `discard:`, `ctx.join(x)` заменил пару `await x(); ctx.check();`, голый
     вызов остался верным для тела, которое ничего не спрашивает у контекста.
     Переписаны темы, оба `README`, шесть зеркал, примеры, шаблоны IDE и
     `CHANGELOG.md`.

   Тестов 501 → 513, гейт §6 пройден целиком **здесь**, а не по отчёту
   исполнителя (и сьюта примера, 5 зелёных). Три расхождения со спекой — в
   конце самой спеки.

   **Работу делал `codex` в изолированном клоне, пять кругов; приёмка своя.**
   Что из этого стоит помнить, кроме самой работы:

   - **отчёт исполнителя — заявка.** Первый круг честно сказал `done: false`:
     песочница не дала запустить ни одной проверки. Мои прогоны нашли два
     падавших теста и два замечания анализатора, которых в отчёте быть не
     могло;
   - **песочница `codex exec` без `-c
     sandbox_workspace_write.network_access=true` не даёт открыть сокет**
     (`Failed to create server socket`), поэтому `fvm flutter test` не идёт
     вовсе, а `analyze` умирает на разрешении зависимости. И `.git` она
     закрывает на запись при любых настройках — коммитить приходится снаружи;
   - **`run.sh` скилла падал на macOS-овом bash 3.2** —
     `MODEL_ARG[@]: unbound variable` на пустом массиве под `set -u`; а
     `codex exec` в фоне без `< /dev/null` вставал на `Reading additional
     input from stdin...`. Обе, и сеть тоже, починены в самом скилле
     (`~/.claude/skills`, `150d78a` и `8b89719`), так что заново их искать не
     придётся. Задания и логи той работы лежали в песочнице сессии и с
     рестартом исчезают — всё, что из них стоило сохранить, в скилле и здесь;
   - **он выходит за рамки в сторону регламента**: правил `CHANGELOG.md` и
     `docs/handoff.md`, хотя они были прямо отложены. Откатывать пришлось
     руками;
   - **он удаляет объяснения вместе с кодом, который они объясняли** — в том
     числе те, что объясняли оставшееся. Ловится только чтением диффа.

   До того — **2026-09-07, `3b11872`: вторая заявка потребителя по
   TT-2375 сделана целиком.** Спека —
   `docs/records/2026-09-07[1]-timeout-diagnostics-design.md` (`61d2f16`),
   форму выбрал владелец: **B + C**. Что уехало:

   - **текст истечения строится в колбэке таймера, а не в обработчике.** Оба
     ожидания `scope_coordination.dart` перечисляют то, что не было завершено
     **в момент истечения**; пустой список у детей и `[holder completed]` у
     очереди ключа больше не получаются;
   - **`ScopeObserver.onTimeout` получает `TimeoutException` и стек** —
     ломающая; `ScopePrintObserver` печатает сообщение ошибки за своей
     строкой, `ScopeCompositeObserver` пробрасывает оба аргумента;
   - **`ScopeConfig.timeoutReportsEnabled`** выключает отчёт пакета в
     `FlutterError` и только его; наблюдатель слышит истечение всегда;
   - **ошибка именуется один раз** — в `AsyncScopeParent`, — и уходит и
     наблюдателю, и в отчёт, и в колбэк вызывающего; собственный отчёт
     `AsyncScopeCoordinator.waitForChildren` удалён как дубликат;
   - отчётность сведена в `notifyTimeout` и `reportTimeout` рядом с
     `notifyObserver` (`scope_config.dart`).

   **Три расхождения со спекой — в конце самой спеки**, §6. Главное из них:
   правило «пустой снимок — не истечение» не понадобилось, оно уже закрыто
   поздней проверкой `finished`.

   Тестов 495 → 501, гейт §6 пройден целиком (и сьюта примера, 5 зелёных).
   **Заявка закрыта не вся:** третья просьба (одна причина — два-три отчёта)
   закрыта наполовину и намеренно, вердикт в §4 спеки. **0.13.1 с этой
   правкой решено не выпускать** — решение владельца от 2026-09-07, разбор в
   «Решениях владельца, которые действуют дальше».

   До того — **2026-09-07: сама заявка.** Файл лежит у потребителя:
   `tez-taxi-monorepo`, `.claude/worktrees/tt-2375-scopo-ladder/docs/records/`
   `2026-09-07-TT-2375-scopo-timeout-diagnostics-request.md`. Потребитель на
   0.13.0, но разбирать по ней можно `main`: `git diff v0.13.0..HEAD` по
   `scope_coordination.dart` и `async_scope_parent.dart` пуст — код тот же.
   Три просьбы: **(1)** список незавершённых детей в отчёте об истечении
   приходит пустым; **(2)** отчёт об истечении нельзя взять на себя ни одним
   из четырёх хуков; **(3)** одна причина даёт два-три независимых отчёта.
   Сами таймауты чинить не просят — просьбы только про то, что видно снаружи.

   **Разбор на входе. (1) подтверждена прогоном, и шире, чем в заявке.** Оба ожидания
   `scope_coordination.dart` строят текст истечения **в обработчике**, а не в
   момент истечения, и между этими двумя точками лежит несколько микрозадач.
   У детей это даёт `[]`: `unregister()` помечает `_completer` завершённым
   синхронно, а `.wait` доносит это на пару хопов позже — так что ребёнок
   успевает выпасть из фильтра, но не успевает снять само истечение. У очереди
   ключа тот же дефект мягче: список полный, но статусы читаются поздно, и
   сообщение выходит `newcomer couldn't wait to get access to [key]: [holder
   completed]`. Обе строки воспроизведены детерминированно — `FakeAsync` не
   выпускает микрозадачи, пока его не попросят, а таймер лимита живёт в
   корневой зоне и до фейкового времени ему дела нет; в проде ту же щель даёт
   пачка таймеров, истёкших в один заход. Пробники прогнаны и удалены,
   в дереве их нет.

   **(2) верна как есть.** Четыре хука (`onScopeKeyTimeout`,
   `onInitCancellationTimeout`, `onDisposeScopeTimeout`,
   `onWaitForChildrenTimeout`) зовутся **после** безусловного
   `FlutterError.reportError` — `async_scope_core.dart:869`, `:1254`, `:1382`;
   заменить отчёт нельзя ни в одном. Уровнем ниже
   `AsyncScopeParent.waitForChildren` умеет ровно это и говорит об этом прямо
   (`async_scope_parent.dart:94-118`). `doc/debug.md:316-320` обещает
   «unless a callback of yours takes that place» так, что читается это как
   общее правило, хотя следующей фразой сужается до одного случая.

   **(3) — самая мягкая, и (1) закрывает её наполовину:** непустой список уже
   называет виновника, а каждый из трёх отчётов называет своё звено, так что
   цепочка читается по именам. Общий идентификатор причины требует, чтобы три
   независимых ожидания в трёх элементах знали о себе общее — а знают они друг
   о друге только через реестр детей.

   До того — **2026-09-05, `443ce86`: `discarded_futures` выключен, и
   девять `unawaited`, стоявших только ради него, убраны.** Перед этим в тот
   же день — правка идиомы инициализации: **приобретение зовут напрямую, а не
   через `ctx.wait`** (`5364476`), по спеке
   `docs/records/2026-09-05[1]-init-wait-family-design.md` (`adb5262`).
   Половина спеки сделана, остаток ждёт `solo` — «Что дальше».

   До того — **работа 2026-09-04: инициализация переведена с асинхронных
   генераторов на `Future` с контекстом, и стрима в ней не осталось нигде.**
   Подробности в «Где мы сейчас»; спеки, планы и вердикты —
   `docs/records/2026-09-04[1]-init-context-design.md`,
   `docs/records/2026-09-04[2]-init-context-plan.md` и
   `docs/records/2026-09-04[3]-streamless-internals-design.md`.

   **Чем кончилось: 0.14.0 выложена на pub.dev 2026-09-09** по явной просьбе
   владельца, после полного гейта §6. Решений по ней не осталось.

   Дальнейшие пункты этого раздела — про прошлые ревью; они закрыты и
   оставлены как история.

6. Сверься со старым: ниже — состояние ревью на 2026-09-01.
3. **Шестое ревью проведено 2026-08-31, два независимых прохода. 29 находок,
   и оба списка исчерпаны: у каждой находки вердикт на месте.**

   - `docs/records/2026-08-31[3]-project-review.md` — **Fable, 26 находок**
     (0 Critical, 2 High, 5 Medium, 19 Low/Info). Прогнано всё: сьюта трижды,
     гейт целиком в копии, 18 мутаций (2 выжили и стали находками), 6 зондов.
     Обе High подтверждены зондами и **закрыты 2026-08-31**, `65614fc`;
     `M1` и `L5` — `f377aea`; `M5` — `8f388fb`; `M2`–`M4` — `cfab7de` и
     `b4c8f48`; весь хвост Low и Info — `f07611f` и `9e8cace`;
   - `docs/records/2026-08-31[2]-project-review.md` — **Codex, 3 находки**
     (0 Critical, 1 High, 1 Medium, 1 Low). Запустить не смог ничего:
     песочница не дала FVM писать в свой кеш. Все три получены чтением; его H1
     подтверждена зондом уже здесь и **закрыта тем же коммитом**; M1
     подтверждена зондом и **закрыта 2026-08-31**, `f377aea`; L1 закрыта
     сужением дартдока, `f07611f`.

   **Где они сошлись — то место закрыто первым, и оно того стоило.** `M1` у
   обоих был про одно: зависимость **чужой реализации** на пути разбора. Codex
   нашёл чтением дыру в контракте, Fable — мутацией дыру в покрытии. Обе
   половины закрыты 2026-08-31 (`f377aea`), и поиск соседа по правилу §8 вынес
   оттуда третью, которой не было ни у кого: **конкурентная группа с чужой
   ветвью, бросающей до возврата потока, вешала обход молча** — ни ошибки, ни
   выхода, ни конца, и так же на входе. Вердикты — в самих ревью.

   **Три находки Fable говорили, что прошлое закрытие было неполным** (M2, M3,
   M4) — и все три подтвердились: дефект каждый раз выживал там, куда не
   заглянул список, написанный руками. Отсюда `test/published_docs_test.dart`:
   обе проверки читают **каждый** публикуемый документ и его зеркало.

   **Читать вердикты, а не только статусы.** Четыре спорят со своей находкой:
   `M4` (утечка на рестарте страницы не воспроизводится), `L5` (усечённый путь
   у отказа — не расхождение), `L15` (два пункта оставлены намеренно, с
   причиной у каждого, а третий стал решением владельца) и `L1` Codex
   (открытие второго факта в API — решение владельца).

4. **Волну этих правок отревьюировали отдельно, тем же вечером 2026-08-31,
   теми же двумя исполнителями: 10 находок в 8 местах, ни одного Critical и
   ни одного High. Оба списка исчерпаны 2026-09-01 — у каждой находки вердикт
   на месте.** `M1` обоих — `5e5d9f6`; `M2` Codex — `dd317af`; `M3` Codex —
   `3e0e863`; `L1` обоих — `06408f8`; `L2` Codex — `1cb3e23`; `L3` Codex и
   `Info-2` Fable — `0d6a310`; `Info-1` Fable — `78fbe11`.

   - `docs/records/2026-08-31[4]-post-wave-review.md` — **Fable, 4 находки**
     (1 Medium, 1 Low, 2 Info). Гейт §6 зелен целиком в копии, сьюта 487,
     19 мутаций-откатов (18 поймано, 1 выжила и стала находкой), 4 зонда;
   - `docs/records/2026-08-31[5]-post-wave-review.md` — **Codex, 6 находок**
     (3 Medium, 3 Low). **Впервые прогнал всё сам:** копию ему выдали с
     разложенным `.fvmrc` и прогнанным `pub get`, а песочнице открыли на
     запись кеш FVM — отказов оттуда больше не было. Прежние его проходы
     (`2026-08-20[1]-project-review.md`,
     `2026-08-31[2]-project-review.md`) были получены чтением целиком.

   **Оба сошлись на `M1`, независимо, разными зондами.** Охрана `dispose∥init`,
   поставленная волной, стоит только на входе
   `ScopeAutoDependencies.dispose()`, а тот же обход достижим через публичный
   `root` и прямой `ScopeDependency.dispose()` — там утечка H1 живёт целиком:
   диспозер, зарегистрированный после `await`, теряется навсегда, а
   `disposalRequired` после инициализации лжёт. Это остаток High-находки —
   **закрыт 2026-09-01, `5e5d9f6`:** охрана теперь стоит на обеих дверях, и
   довесок об этом стоит в вердикте самой `H1`. Второе совпадение мельче:
   `published_docs_test` сканировал не все публикуемые Markdown
   (Fable Info-2 = Codex L3) — теперь берёт `CHANGELOG.md` и весь
   `example/**`, `0d6a310`.

   **Два теста волны не держали того, что закрыли, и разошлись концы.**
   `full_scope_counter_test` зелен при точном откате `b4c8f48` — тест проверяет
   свойство, которое держалось и до правки (Codex M3): удержать порядок двух
   строк в этой сьюте нечем, поэтому исправлено обещание. А охрана
   `select`-после-`cancel` не держалась ничем (Fable L1) — держатель написан,
   `06408f8`.

   **Читать вердикты, а не только статусы.** Два спорят со своей находкой:
   `M3` Codex закрыт правкой обещания, а не кода (порядок двух строк в демо
   удержать нечем — инициализатор листа отменить нельзя), и `L1` Codex закрыт
   не поколением доставки, а счётчиком успешных: сравнивать значения значило бы
   звать пользовательский `compare` из ветки отказа.

   **С чего браться теперь:** очередь ревью пуста, `docs/backlog.md` пуст.

   **Открытых решений владельца нет:** последнее, публикация 0.14.0,
   закрыто 2026-09-09 — выложена. Прежние закрыты тоже: публикация
   0.13.0 состоялась 2026-09-01 (тег `v0.13.0` на `f0abf0e`), ещё два — 2026-08-31,
   они живут в «Решениях…» — `disposeScope` после упавшего `initScope` и сьюта
   примера в архиве. Работа не из списка ревью начинается с записи дизайна и
   согласия владельца («Что дальше»).
5. **Волну, закрывшую те десять находок, тоже отревьюировали — 2026-09-01,
   двумя исполнителями, и она вернулась чистой.** Это третий проход подряд по
   одному коду и первый почти пустой: одна находка Info на двоих, закрыта
   (`706b837`). Очередь снова пуста.

   - `docs/records/2026-09-01[1]-post-wave-review.md` — **Fable, 1 Info**.
     Гейт §6 целиком, сьюта 495 дважды, 8 мутаций, 11 зондов;
   - `docs/records/2026-09-01[2]-post-wave-review.md` — **Antigravity (`agy`)
     на Claude Opus 4.6, 0 находок**. Гейт целиком, 5 мутаций.

   **Бриф был написан иначе, чем прежние, и, похоже, это и дало результат:** в
   нём названы **два слабых места самого автора волны** — охрана `5e5d9f6`
   подтверждена рассуждением, а не зондом, и счётчик `06408f8` проверен на
   одной форме реентерабельности. Оба ревьюера пошли туда, и обе точки
   устояли. Fable при этом **прогоном опроверг собственное чтение**
   `runStreamGuarded`, предсказывавшее третью дверь.

   **Два прохода из четырёх не состоялись, и это стоит помнить.** Codex на все
   модели получал `not supported when using Codex with a ChatGPT account` при
   живом логине. А `agy` на `gemini-3.1-pro-high` **выдал вымысел**: «489
   тестов, все зелёные» (число взято из сообщения коммита `dd317af`, в дереве
   495), «analyze — no issues» поверх собственных трёх замечаний, «зонды
   зелёные» при двух упавших по таймауту. Тот проход в `docs/records/` не
   внесён; разбор — в оговорках `2026-09-01[2]-post-wave-review.md`.
   **Числа прогона у чужого исполнителя проверяй своим прогоном** — это
   дешевле, чем поверить.
6. **Ветка `main`, других веток и worktree нет. Всё запушено.** Проверяй сам:
   `git log --oneline origin/main..HEAD` — должно быть пусто.
7. **Гейт §6 пройден целиком 2026-09-09 на голове `main`**, на полу 3.27.0 —
   числа в «Проверках»; ту же голову подтвердила CI, обе job. Номер коммита
   сюда не пишется: он устаревает раньше,
   чем его прочтут. **Сьюта примера** (5 тестов в `example/scopo_demo`) в
   восьмёрку §6 не входит, но прогнана и локально — зелёная; её же
   гоняет job `gate`.
8. **CI — `.github/workflows/ci.yml`, две job.** `gate` — тот же гейт плюс
   сьюта примера и `git status --porcelain`, на чистом клоне, на полу 3.27.0.
   `ceiling` — на текущем `channel: stable`, только `analyze` и тесты, и она
   **`continue-on-error`**: это ранний сигнал о новом стабильном SDK, а не
   обещание. Красная `ceiling` прогон не валит — смотри её вывод и решай с
   владельцем. Смотреть прогоны: `gh run list`, `gh run view <id> --log-failed`
   (`gh` здесь авторизован как `vi-k`). **На голове зелена.** В истории есть
   один красный `gate` — `2243baf`, 2026-08-31: туда уехали три замечания
   `analyze`, потому что вывод уже прогнанной проверки не был прочитан перед
   коммитом. Закрыт следующим коммитом; помнить стоит не отказ, а то, чем он
   вызван.
9. **Что открыто — три записи**, все в «Открыто, вне списка ревью», все —
   принятые границы с причиной, и ни одна не ждёт решения владельца:
   неограниченный `await _initCompleter.future`, чтение приватного
   `_initSucceeded` соседнего слоя и два набора селекторов у зависимого,
   перестроенного дважды за кадр. **Долга в разделе не осталось:** три записи
   закрыты 2026-09-01 — `7985fb6`, `47bbb95`, `66b1745`.

   И два ограничения, которые закрыть нельзя по построению: пропавший
   `scheduleFrame()` виджет-тесту невидим (`pump()` рисует кадр безусловно), а
   release-ветки за ассертом не проверяются, потому что `flutter test` всегда с
   ассертами.
10. **Тулчейн — Flutter 3.27.0 через fvm**, он же заявленный пол. Всё гонять
    через `fvm`. Прогнал другим SDK — сразу `fvm flutter clean && fvm flutter
    pub get`, иначе следующий прогон падает на `ink_sparkle.frag`. Подробности —
    в «Тулчейн и окружение».
11. **Если помнишь пакет по прежним именам** — их нет с 2026-08-18. Главное:
    `initAsync`/`disposeAsync` разошлись на `initScope`/`disposeScope` (слой
    скоупа) и `initStateAsync`/`disposeStateAsync` (слой состояния);
    `buildOnInitializing` → `buildOnProgress`; `DepHelper` →
    `ScopeDependencyHandle`; `ScopeStateNotifier.equals` → `shouldNotify`
    **с обратным смыслом**. Полная таблица — в
    `docs/records/2026-08-18[2]-naming-plan.md`. Правило, по которому имена
    выбирались: голый глагол значит, что метод делает названное, `on…` — что он
    вызывается, когда это случилось.
12. **История — не здесь.** Чем и когда закрыта конкретная находка — в
    вердикте самой находки, в файле ревью. Разборы законченных эпизодов,
    таблицы волн, список сделанного — в
    `docs/records/2026-08-30[1]-handoff-history-log.md`. Спеки, планы и отчёты
    — там же, в `docs/records/`.

## Где мы сейчас

**Инициализация работает на ядре `async_job` — своей копии контекста задачи
больше нет. Версия 0.14.0 выложена на pub.dev 2026-09-09** по явной просьбе
владельца, гейт §6 перед публикацией пройден целиком, архив 276 КБ,
`Package has 0 warnings`.
Послеволновое ревью 2026-09-09 принесло двадцать две находки, и все они
закрыты — разбор в §1 выше и в
`docs/records/2026-09-09[5]-post-wave-review.md`. Стрима в инициализации нет
нигде — ни в форме, ни под капотом.

**Всё запушено: `main` и `origin/main` совпадают.** Работа 2026-09-09 —
переход на ядро и записи о нём — отправлена **2026-09-09 по просьбе
владельца**; проверять состояние надо самому, `git log --oneline
origin/main..HEAD` пуст. Машина эту голову увидела: прогон `34322013142`,
результат — в «Проверках». (На 0.14.0 `ceiling` краснел `discarded_futures`,
линт выключен 2026-09-05, `443ce86`, разбор — в «Тулчейн и окружение».)
Локальный гейт §6 на нынешней голове пройден целиком 2026-09-09,
см. «Проверки».

**Поверх 0.14.0 легла волна 2026-09-05, три части.** (1) `adb5262` — спека
`2026-09-05[1]-init-wait-family-design.md`: `ctx.wait` вокруг приобретения
отключает собственную сеть движка, потому что та ловит только **возвращённое**.
(2) `5364476` — правка по спеке: приобретения зовут напрямую во всех темах,
обоих `README`, пяти зеркалах, двух примерах и шаблонах редакторов; правило
записано в тему `AsyncScope` и в дартдок; запись добавлена в `CHANGELOG.md`
0.14.0. (3) `443ce86` — `discarded_futures` выключен, девять обёрток
`unawaited`, стоявших только ради него, убраны. Остаток работы по спеке закрыт
переходом на `async_job` (`bc914c8`) — см. «Что дальше».

- **Форма (половина A):** инициализация во всех семействах — обычная
  `async`-функция: `ctx.progress(x)` вместо `yield Progress(x)`, `return value`
  вместо `yield Ready(value)`, отмена кооперативная через `ScopeInitContext`.
  Спека и план: `docs/records/2026-09-04[1]-init-context-design.md` и
  `docs/records/2026-09-04[2]-init-context-plan.md`.
- **Внутренности (половина B):** движок сам запускает тело — ни подписки, ни
  `asyncMap`, ни адаптера; дерево зависимостей ходит обычными вызовами
  `init(ctx, onStep)` и `dispose(onStep)`. `_mergeStreams` и
  `runStreamGuarded` удалены целиком. Спека:
  `docs/records/2026-09-04[3]-streamless-internals-design.md`, вердикты по
  пяти расхождениям — в её конце.
- **Публичная поверхность:** ушли девять имён (шесть `*InitState`-классов,
  `asStream`, `ScopeAutoDependenciesStream`, `ScopeDependencyDisposalCancelled`),
  `ScopeInitCallback` потерял параметр типа прогресса, `ScopeDependency.init` и
  `.dispose` сменили сигнатуры; появились `ScopeInitContext`,
  `ScopeInitCancelled` и `ScopeInitHandle`.
- **Что стало возможным:** значение, произведённое после отмены, отдаётся в
  `dispose`, а не теряется; отмена приходит броском, так что `catch` работает и
  флаг «передано» не нужен.
- **Что исчезло само:** три диагностики (инициализация без готовности, второй
  `Ready`, отказ после готовности) — не удалены, а стали непредставимыми.
- **Гейт §6 пройден целиком** перед каждым мержем; на последнем: 495 тестов,
  analyze ×3 чисто, format 113/0, doc 0/0, publish 0 warnings, переводы 16.

**Все 29 находок шестого ревью закрыты 2026-08-31**, у каждой
вердикт в своём файле — там же сказано, чем находка ошибалась, если ошибалась,
и что оставлено намеренно. **Но очередь не пуста:** волну этих правок
отревьюировали отдельно тем же вечером, и она принесла 10 находок в двух
списках (`2026-08-31[4]-post-wave-review.md` и
`2026-08-31[5]-post-wave-review.md`) — 8 разных мест, ни одного Critical и ни
одного High. **Оба списка исчерпаны 2026-09-01:** у каждой из десяти находок
вердикт в своём файле. **Волну этих закрытий отревьюировали снова, 2026-09-01,
и она вернулась чистой** — одна находка Info на двух исполнителей, закрыта
(`706b837`): `2026-09-01[1]-post-wave-review.md` и
`2026-09-01[2]-post-wave-review.md`.
Списки шести предыдущих ревью
исчерпаны целиком:
18 + 43 + 44 + 52
находки кода (157), 20 находок аудита имён, 7 потребительских
(`2026-08-23[1]-consumer-review.md`), 26 по двум ревью 2026-08-28 и 9
послеволновых (`2026-08-29[1]-post-wave-review.md`). У каждой вердикт в своём
файле — в том числе там сказано, чем находка ошибалась, если ошибалась.

**На pub.dev — 0.14.0**, опубликована 2026-09-09 по явной просьбе владельца
(архив 276 КБ, `Package has 0 warnings`; тег `v0.14.0` на `b0c3830`).
До неё 2026-09-01 вышла 0.13.0 (тег `v0.13.0` на `f0abf0e`, архив 260 КБ),
2026-08-24 — 0.12.0 и 0.11.0, 2026-08-23 — 0.10.0.

**Раздел 0.13.0 в `CHANGELOG.md` закрыт и выпущен — тридцать пунктов**,
`pubspec.yaml` держит 0.13.0, новый раздел не открыт: его заводят, когда
появится первая правка следующей версии. Что уехало в релиз:

- **четыре правки помечены ломающими.** Две ранние: разбор контейнера уехал из
  `onProgress` в `onDisposalProgress`, и второй `init()` на неразобранном
  контейнере отказывает независимо от того, держит ли дерево что-нибудь. Две от
  2026-08-31, из High шестого ревью: `disposeStateAsync` выполняется после
  упавшего `initStateAsync` (диспозер обязан принимать частично
  инициализированное состояние), и `dispose()` контейнера во время идущего
  `init()` отвергается `StateError`-ом;
- **ломающими не помечены, но поведение меняют, ещё три**: второй одновременный
  `dispose()` присоединяется к идущему обходу, а не запускает свой;
  `onDisposalProgress` отправляет контейнер в момент возврата диспозера, а не
  поток обхода; `onError` упавшего освобождения — тоже контейнер, и приходит он
  сразу за своим входом, а не в конце обхода. К ним стоит отнести и пятое с
  шестым: `ScopeDependencyException.toString()` печатает лишнюю строку, когда
  несёт подсказку, и отказ **чужого корня** приходит наблюдателю названным
  (`ScopeDependencyException` с именем зависимости), а не сырой ошибкой.
  Метки на них нет намеренно — проза каждого пункта говорит, что
  меняется; **ставить ли метку, решает владелец**, и у `0.x` это документация,
  а не версионирование: минорный бамп слот ломающих несёт в любом случае.

**Режим работы: окно ломающих правок закрыто тремя публикациями подряд.** До
0.10.0 ломающая правка стоила ноль сверху, и семь их уехало намеренно. С тех
пор она стоит **минимум минорного бампа** — не обязательно мажорного, пока
пакет `0.x`: у `0.x` минорный бамп сам несёт слот ломающих, и 0.11.0 прошла
ровно по этому правилу. То же относится к переименованиям. Это первое, что надо
сказать владельцу, если он попросит переименование или новый вариант в
публичном `enum`.

**Последняя правка в `lib/` до заявки 2026-09-07 — 2026-09-01, `66b1745`: чужой `Stream`,
отказывающийся быть прослушанным, больше не вешает обход.** Сосед предыдущей,
строкой ниже в том же методе: охрана там накрыла обход цепочки, а подписки на
ветви создаются под ней, и `listen` чужого `Stream` бросить вправе. Ценная
половина здесь — уборка: бросок на третьей ветви оставляет две первые
подписанными и работающими, они пишут в `controller.add`, и контроллер,
закрытый под ними, превращает один отказ в два. Поэтому подписанное сперва
отменяется, и только потом отвечается ошибка. Тест ставит отказывающую ветвь
**второй** — фикстура на первой позиции до этой половины не дошла бы.

**Перед ней — 2026-09-01, `47bbb95`: чужая ветвь, бросающая
из геттера `state`, больше не вешает обход.** Остаток `f377aea` и вторая из
двух записей, закрытых в этот день. `_guarded` там встал вокруг `dep.init()`, а
фильтр перед ним — `where((dep) => dep.initializationRequired)` — обходится
внутри `onListen` слитого потока, откуда бросок не уходит никуда: ни ошибки, ни
выхода, ни конца, и соседняя ветвь держит взятое. Охрана поставлена вокруг
обхода цепочки, а не вокруг геттера: опасно вообще исполнять чужой код из
`onListen`, в какой бы части цепочки он ни стоял. Ошибка отвечается из
микротаска — контроллер `sync: true`, и событие, добавленное изнутри
`onListen`, приходит к подписке, которую `listen` ещё не вернул. Тест сперва
падал так же, как падает дефект: `TimeoutException after 0:00:10`.

**Перед ней — 2026-09-01, `7985fb6`: группа больше не списывает ребёнка,
который инициализируется в обход неё.** Первая из двух записей. Охрана
`dispose()` спрашивает `_initializing` того узла, с которого начали, и это
полный ответ только пока дерево ведёт один каллер; группа, чей ребёнок был
проинициализирован мимо неё, своего флага не несёт. Отказ поставлен в
`_markNothingToDispose` — единственное место, где отметка вообще пишется.

**Перед ней — 2026-09-01, `1cb3e23`: отказ говорит, что
даст отмена, в том режиме, в котором отказывает.** `L2` Codex. `StateError`,
которым `dispose()` во время `init()` отсылает каллера отменять инициализацию,
плоско утверждал, что отмена «освобождает построенное». Это правда про режим по
умолчанию и ложь про opt-out: контейнеру с выключенным `autoDisposeOnError`
обещано обратное — полупостроенное дерево сохраняют, а отмена размонтирует и
останавливается. Средняя часть сообщения теперь спрашивает флаг. Сам факт был
закреплён с тех пор, как opt-out написан; не хватало того, чтобы о нём знало
сообщение.

**Перед ней — 2026-09-01, `06408f8`: откат в `select` перестал затирать
успешную доставку.** `L1` обоих ревьюеров, один файл. Значение пишется до
вызова слушателя (чтобы вложенное уведомление не начинало своего обхода) и
кладётся назад, если слушатель бросил, — но безусловный откат затирал
вложенную доставку, которая успела пройти. Считаются успешные доставки, а не
поколения: сравнивать значения значило бы звать пользовательский `compare` из
ветки отказа. Тем же коммитом появился держатель на охрану
`select`-после-`cancel`, которую до того не держало ничто.

**Перед ней — 2026-09-01, `dd317af`: контракт частично
инициализированного состояния виден на всех страницах, где нужен.** `M2` Codex,
найдена не чтением, а генерацией документации. Три состояния, которые читатель
и наследует (`LiteScopeState`, `ScopeCoreState`, `ScopeState`), собирают
`initStateAsync` и `disposeStateAsync` в свой блок переопределений и давали
каждому однострочный дартдок — а дартдок на `@override` не добавляет строку к
странице, он заменяет унаследованный текст целиком. Однострочники сняты:
`public_member_api_docs` документа на переопределении не требует, это проверено
прогоном до правки. `initStateAsync` — тот же дефект именем левее, в тех же
шести местах, и правится тем же движением. Держит это
`test/published_docs_test.dart`; проверено генерацией, тем же способом, каким
находка получена. Тема `LiteScope` получила само правило, зеркало
заштамповано. Тестов 488 → 489.

**Перед ней — 2026-09-01, `5e5d9f6`: охрана `dispose()` во
время `init()` встала на вторую дверь.** `M1` обоих послеволновых ревью, и
единственная точка, на которой оба исполнителя сошлись независимо. Волна
закрыла `H1` охраной на `ScopeAutoDependencies._runDispose` — двери самого
контейнера; вторую дверь он отдаёт сам, публичным `root`, и обход, начатый
оттуда, воспроизводил находку целиком: запаркованный лист помечался как
«держать нечего», группа потом пропускала его по `disposalRequired`, и диспозер,
зарегистрированный мгновением позже, не звал никто и никогда. Теперь
`ScopeDependencyMixin.dispose()` отвергает вызов при поднятом `_initializing`,
до джойнера и до любой отметки; между публичной дверью и запаркованным листом
инициализируется каждый узел, так что третьей двери нет. Ловушка закрытия `H1`
— контейнер, отказывающий сам себе из `finally` собственного `init()` — здесь
не сработала: к этому моменту `yield*` уже размотал внутренний генератор.
Пункт `CHANGELOG` не удвоен, а расширен: это одна охрана на двух дверях.
Тестов 487 → 488.

**Перед ней — 2026-08-31, `a94d348`: комментарий у охраны
`if (_initSucceeded)` в `async_scope_core.dart`.** Поведение не менялось: там
записано решение владельца о `disposeScope` (см. «Решения владельца…»), чтобы
следующий проход не «починил» это заодно — 31 августа к тому шло.

**Волна перед ней — 2026-08-31, `f07611f` и `9e8cace`: весь хвост Low и Info,
девятнадцать находок разом.** В `lib/` — одиннадцать мелких правок, каждая со
своим тестом и своей мутацией: подтверждённая утечка слушателя у
`ScopeNotifier`, чей первый `value` отказал; `select`, считавший бросившего
слушателя получившим значение; `StateAsNotifier`, строивший новый нотификатор
из хвоста собственного `dispose()`; двойная отчётность отказа при снятии с
дерева во время `close()`; запоздалый `notifyDependents()`, бивший голым
ассертом фреймворка; диагноз про чужую ошибку у `buildOnWaiting() == null`;
ассерт против повторно использованного контроллера; `cancel()` композита,
бросавший обход на непокорном члене. Остальное — документы, примеры и CI:
README говорил «Nine families» против собственной таблицы из восьми,
`RecordingObserver` в разделе Testing не существует в пакете, у обеих job CI не
было `timeout-minutes`. **Три пункта Info оставлены намеренно** — причина у
каждого в вердикте `L15`. Тестов 478 → 487.

**Перед ней — 2026-08-31, `cfab7de` и `b4c8f48`: три Medium вне `lib/`.**
Команды установки IDE-шаблонов в теме `base` брали из pub-cache версию по
лексикографическому `sort` (`0.9.6` после `0.13.0`); десять фрагментов тем
`AsyncScope`/`AsyncDataScope` звали параметры именами, которых нет с
2026-08-18, — «правильный» образец из темы не компилировался; демо семейства
`Scope` регистрировало диспозер после `await`. Все три — остатки исправлений,
сделанных однажды по списку, написанному руками, поэтому появился
`test/published_docs_test.dart`: он спрашивает каждый публикуемый документ и
каждое зеркало. Два уточнения к находкам записаны в вердиктах: утечка, которую
M4 обещает на рестарте страницы, **не воспроизводится** (инициализатор листа —
обычная `async`-функция, отменить её нельзя, диспозер регистрируется мгновением
позже и обход его застаёт), а вот у демо `LiteScope` того же имени контроллер
не освобождался **вообще никогда** — это сосед, найденный по §8 и починенный
там же. Тестов 476 → 478, сьюта демо 3 → 5.

**Перед ней — 2026-08-31, `8f388fb`: отвергнутый `close()`
больше не хоронит разбор.** `close()` просит перестройку раньше, чем что-либо
освобождает, а `markNeedsBuild()` во время сборки или при залоченном дереве
фреймворк отвергает — то есть ровно там, где `close()` зовут из сборки потомка
или из `dispose()` соседнего `State`. Отказ запечатывался в `_closeCompleter`,
и к нему присоединялся всякий следующий вызов, включая разбор при снятии с
дерева: не выполнялось **ничего** — ни `disposeScope`, ни отмена `initScope`,
ни `disposeStateAsync`, ни выход из очереди `scopeKey`. Теперь охраной накрыт
префикс прогона, и его отказ снимает мемо; сам отказ по-прежнему уходит
вызвавшему. Из трёх путей находки взят (а): отсрочка (б) сработала бы на
каждом штатном разборе — проба показала, что тот идёт внутри кадра
(`isBuilding=true`, `persistentCallbacks`). Тестов 475 → 476.

**Перед ней — 2026-08-31, `f377aea`: точка, где сошлись оба
ревьюера.** `ScopeDependency` — публичный интерфейс, поэтому чужой `dispose()`
вправе бросить **до того, как вернёт поток**; свои так не умеют — оба их обхода
`async*`, а те не бегут до подписки. У корня контейнера такой бросок уходил
мимо канала (наблюдатель слышал вход и не слышал ничего за ним, а обещание
«never re-throws» не держалось); у **конкурентной** группы — хуже и тише: обход
вставал навсегда, без ошибки, без выхода и без конца, и точно так же на входе.
Причина у второй одна на оба пути — `_mergeStreams` спрашивает у ветвей потоки
внутри `onListen` своего контроллера. Плюс `L5`: отказ чужого корня теперь
назван, как у двух других отправителей. Сосед найден пробой, а не чтением, — и
в находках его не было. Пять тестов, шесть мутаций. Тестов 470 → 475.

**Перед ней — 2026-08-31, `65614fc`: три High шестого ревью.**
Изменение inherited-зависимости, съеденное ожидающим уведомлением
(`didChangeDependencies` теперь поднимает `_forceRebuild`); `disposeStateAsync`
после упавшего `initStateAsync` — теперь в том же порядке, что и на готовом
пути; `dispose()` контейнера во время идущего `init()` — отвергается
`StateError`-ом, последняя из четырёх диагоналей конкурентности. Вердикты — в
самих файлах ревью. **Две ломающие правки из трёх**, обе в `CHANGELOG.md`
0.13.0. Тестов 465 → 470.

**Перед ней — 2026-08-31: второй запуск над `late final`
говорит, почему ему отказали.** Контейнер, чей разбор дошёл до конца, можно
инициализировать заново, и он заново выполняет каждый инициализатор над тем же
объектом; поле `late final`, которому первый запуск присвоил, второго
присваивания не принимает. Наружу выходил голый `LateInitializationError` с
именем зависимости — то есть ошибка читалась как промах в собственном
инициализаторе вызывающего. Теперь `ScopeDependencyException` несёт
необязательный `hint`, и контейнер ставит его здесь; больше нигде он не
ставится. Разбор, три пути и почему выбран этот — в записи
`docs/records/2026-08-31[1]-late-final-reinit-design.md`. Оттуда стоит унести
одно: **`LateInitializationError` — не тип.** Настоящий класс — `LateError` из
`dart:_internal`, оба вида отказа (`уже` и `ещё не` проинициализировано) — это
он же, а `e is LateInitializationError` не собирается вовсе. Значит распознать
эту ошибку можно только разбором текста. **Публичным его не сделают, и это
сказано прямо:** запрос ровно об этом — dart-lang/sdk#45655 — закрыт
2021-04-26 ответом «`LateInitializationError` was removed for a reason, we may
throw any `Error` if you fail to initialize», то есть и `LateError` по
`runtimeType` не контракт. Приемлемо это лишь потому, что
режим отказа безопасен: сменится сообщение в SDK — исчезнет подсказка, а
дрейф поймает тест на полу.

**Перед ней — 2026-08-30, `f6b6d2e`: отказ фазы разбора
поехал тем же каналом, что и пара «вход/выход».** Довесок к находке R5
послеволнового ревью, закрытой днём раньше (`7423ed3`, спека
`2026-08-29[2]-disposal-exit-channel-design.md`). Три вещи, которые стоит знать,
если сюда вернутся:

- **пара держится, кто бы ни вёл обход и чем бы обход ни кончился.** Выход
  разбора уехал с потока на тот же канал колбэков, что и вход: одного ручного
  `root.dispose().listen(...)` довольно, чтобы входы приходили вне скобки
  `onDispose`/`onDisposed`, а обход, отменённый на запаркованном диспозере,
  оставлял вход без выхода навсегда при освобождённом ресурсе;
- **отметка отправляется до `yield`, и это несущее решение**: отменённый
  `async*` доигрывает до ближайшего `yield` и не дальше. Перенос отметки за
  `yield` роняет ровно тест на отмену — проверено мутацией. Ставить взвод в
  `catch` вокруг `yield` при этом безопасно: при отмене `async*` выполняется
  `finally`, а `catch` — нет, проверено на 3.6.0 и 3.13.0;
- **отказ отправляют там, где он случился**, поэтому он приходит сразу за своим
  входом, а не в конце обхода. Это изменение порядка событий, и существующий
  тест на бросок переписан под него.

Спека прошла два независимых разбора (Fable, Codex) **до** реализации, и оба
опровергли её первую редакцию; те же два ревьюера, посланные уже по готовому
коду, независимо нашли довесок выше и пробел покрытия — пять из восьми тестов
не краснели при полном откате правки. Обе редакции спеки и что именно
изменилось — в самой спеке.

**Работа, из которой всё это выросло, — отметки на входе в шаг контейнера по
заявке потребителя TT-2375** (2026-08-28, запись
`2026-08-28[1]-step-entry-notification-design.md`). Девять хуков наблюдателя
стали двенадцатью.

**`NavigationNode` здесь больше нет** — вынесен 2026-08-24 по находке U7 в
[vi-k/navigation_node](https://github.com/vi-k/navigation_node), на pub.dev
**0.3.0**. `example/scopo_demo` зависит от него с pub.dev, поэтому правка узла
доезжает до демки только через публикацию; состояние того пакета — в его
собственном `docs/handoff.md`.

**Единственный оставшийся рычаг находки U3** («публичных типов 99») —
разделение барелей, 23 имени из 99, и это решение владельца. Записанная точка
входа отработана 2026-08-24 и дала ноль: шестнадцать имён, которых нет нигде
снаружи, проверены по сигнатурам — убирать нечего. Отчёт —
`2026-08-24[4]-u3-public-surface-report.md`.

## Решения владельца, которые действуют дальше

- **L1 послеволнового ревью правится текстом, а не кодом** (2026-09-09):
  обещание про `FlutterError` снимается из `doc/async_scope.md`, зеркала и
  `CHANGELOG.md`. Отчёт в `_settleFailure` не заводим: он поменял бы и
  обычный отказ тела и дал бы красную строку там, где ошибка уже показана
  через `errorBuilder`;
- **L4 распространяется на второй канал** (2026-09-09): `Cancelled` из
  `dep.dispose` перестаёт идти в `FlutterError`, как уже перестал из
  `ctx.onDispose`. Одно правило на оба канала — потребитель, переложивший
  уборку с одного на другой, не получает обратно красную строку и падающий
  виджет-тест;
- **0.13.1 с правкой диагностики не выпускаем** (2026-09-07): в патч влезал
  только первый пункт заявки — текст истечения, — потому что второй меняет
  сигнатуру `ScopeObserver.onTimeout` и стоит минимум минорного бампа, а
  переключатель `timeoutReportsEnabled` без обогащённого `onTimeout`
  потребителю бесполезен: выключив отчёт, он потеряет тот самый текст, ради
  которого всё и делалось. Владелец решил, что сделанного на 0.14.0
  достаточно. Ветку сопровождения от тега `v0.13.0` не заводили, и `main`
  остаётся единственной линией. Механика на случай, если решение переменится:
  `scope_coordination.dart` в `v0.13.0` побайтово тот же файл, что был у нас
  до правки, так что она ложится туда без конфликтов;
- смена конструктора между владеющим и `.value` у `ScopeModel`/`ScopeNotifier`
  **запрещена ассертом**, а не переносится (волна 4);
- `State.dispose` **закрыт от переопределения** у `LiteScopeState`/`ScopeState`;
  синхронный разбор идёт в `onUnmount()` (волна 4);
- правило имён: **присваиваемый колбэк — короткий глагол** (`dep.unmount`,
  `AsyncScope(unmount:)`), **переопределяемый хук — `onX`**;
- скриншоты для pub.dev не добавляем: статичные кадры технического демо не
  объясняют ценность инфраструктурного пакета и не дают pub points;
- **в русских зеркалах комментарии внутри примеров кода тоже по-русски**;
  исключение — директивы `// ignore: …`, это код, который читатель копирует к
  себе (правило записано в `AGENTS.md` §7);
- ~~**ссылки на темы из `README.md` ведут в репозиторий**~~ — **с 2026-08-23
  ведут на pub.dev**, `documentation/scopo/latest/topics/<Тема>-topic.html`,
  по решению владельца сразу после публикации 0.10.0. В репозиторий они
  показывали, пока темы на pub.dev не открывались вовсе. Девятнадцать ссылок в
  оригинале и девятнадцать в зеркале; абзац над таблицей теперь говорит, что
  `latest` — это всегда последний релиз, а `doc/*.md` в репозитории — то, что
  читают рядом с исходником закреплённой версии. Две вещи, которые стоит
  помнить, если понадобится снова:

  - **на pub.dev эта правка пока не видна, и это не ошибка.** Страница пакета
    показывает `README.md` **из архива опубликованной версии**, а не из
    репозитория, — а ссылки правились после публикации 0.10.0. То есть на
    pub.dev до следующего релиза стоит прежний README с ссылками на
    `github.com/…/doc/*.md`; они рабочие, просто ведут не туда, куда решено.
    Новые приедут с ближайшей публикацией. **Выпускать 0.10.1 только ради
    этого — решение владельца**; довод против: ничего не сломано, а лишняя
    версия остаётся в истории пакета навсегда. Правильный порядок в следующий
    раз — сначала README, потом публикация;
  - **имя темы не равно имени файла.** `doc/full_scope.md` — это тема `Scope`,
    остальные десять совпадают. Имена берутся из `dartdoc_options.yaml`
    (`categories:`) и из `{@category …}` в `lib/`, а не из путей;
  - **после публикации документация выкладывается не сразу, и статус в API об
    этом не говорит.** Пока темы новой версии не выложены, pub.dev редиректит
    **любой** её URL — включая `documentation/scopo/0.10.0/…`, не только
    `latest/` — на предыдущую версию. Выглядит обманчиво: старые темы отвечают
    200 (их отдаёт 0.9.6), а новые (`utils`, `AsyncControllerScope`) — 404.
    `api/documentation/scopo` при этом успел показать `completed`, а редирект
    остался; обхода кеша (`Cache-Control: no-cache`, cache-buster) это не
    снимает, то есть отвечает сам сервер, а не CDN. **Ждать статуса
    `completed` недостаточно, и следить за редиректом тоже не годится:** когда
    выкладка происходит, `latest/…` перестаёт редиректить вовсе и отдаёт
    страницу сама, так что проверка «куда привёл редирект» не сработает ни до,
    ни после. Работает другое — **запросить тему, которой в предыдущей версии
    не было** (у 0.10.0 это `utils` и `AsyncControllerScope`): 404 значит «ещё
    старая», 200 значит «выложена». Номер версии виден и в самой странице;
- **защита написанной руками инициализации — `finally` с флагом, а не
  `catch`** (волна 13): отмена не бросает ничего, поэтому `catch` не
  срабатывает. Так переписаны примеры в темах `AsyncScope`, `AsyncDataScope` и
  `Scope`; в `ScopeModel` остаётся `catch` — `create` синхронный;
- **`disposeScope` не выполняется без выполненного `initScope`, и это решение,
  а не дефект** (2026-08-31). Довод владельца: после сбоя нужна **частичная**
  утилизация, а не полная, и что именно утилизировать — понятно только внутри
  `initScope`. `disposeScope` от выяснения этого свободен и делает полную
  утилизацию. Отсюда идиома «`finally` с флагом» в темах `AsyncScope`,
  `AsyncDataScope` и `Scope` — это не обходной путь, а сам способ. Правило
  **не переносится с соседнего этажа**: `disposeStateAsync` с 0.13.0
  выполняется и после упавшего `initStateAsync`, и это разные слои с разными
  ответами;
- **сьюта примера (`example/scopo_demo/test/`) едет в архив намеренно**
  (2026-08-31). Правило `.pubignore` «сьюта — не документация» к ней не
  относится: она весит 9 КБ против 756 у своей и **сама образец** — показывает
  то, чего не показывает ничто другое в архиве, как довести до конца
  асинхронный разбор в виджет-тесте. Причина записана в самом `.pubignore`,
  рядом со строкой `/test/`, которая привязана к корню;
- пакет не публиковать без явной просьбы.

## Проверки

Полный гейт из восьми команд `AGENTS.md` §6 пройден целиком **2026-09-09, на
голове `main`, и последний раз — непосредственно перед публикацией 0.14.0** (**534 теста** — 513 до правок по ревью того дня, двенадцать
принесли они и девять правки по послеволновому ревью;
`analyze` чист во всех трёх точках, формат — 115 файлов, 0 изменённых,
dartdoc и `pub publish` — по 0 предупреждений, переводы — 16 актуальных), на закреплённом тулчейне (Flutter 3.27.0 /
Dart 3.6.0 через fvm, он же заявленный пол). Сьюта `example/scopo_demo`
прогнана тогда же — 5 зелёных. Прогонялся здесь, а не в песочнице
исполнителя: тот же гейт в его клоне ничего не доказывает про рабочее дерево.
**Машина эту голову видела:** прогон `34358534081` на `e0e650e` зелёный
целиком — и `gate`, и `ceiling`. (Прогон на `b0c3830` отменён своим
преемником — обычное дело при двух пушах подряд, а не отказ.) Каждый из шести коммитов правок по
послеволновому ревью тоже прошёл машину зелёным. Раньше, 2026-09-09, прогон
`34327460973` был красным на обеих job из-за одной запятой — разбор в
«Граблях». То есть и переход на ядро, и правки по ревью проходят не только на
полу 3.27.0, но и на текущем `stable`, где `ceiling` живёт. **Рефакторинг 0.14.0 увёл сьюту с 498 до 495: десяток
проверял сценарии, которые новая форма сделала непредставимыми** — инициализацию, что кончилась не став готовой,
второй `Ready`, отказ после готовности и отмену разбора; каждый случай назван
в вердиктах `docs/records/2026-09-04[3]-streamless-internals-design.md`. Шесть
вернула заявка потребителя 2026-09-07 — четыре на координацию, два на
наблюдателя, — и двенадцать принёс переход на ядро 2026-09-09: шесть в новом
`test/async_job_migration_test.dart` (распространение отказа в параллельной
группе, единственность отчёта об ошибке тела, падение `unattended`, падение
колбэка `onCancel`, и двумя вариантами — освобождение позднего значения ровно
один раз), остальные — в переписанном `test/scope_init_context_test.dart`.
**Девять принесло послеволновое ревью 2026-09-09**: по одному на каждую
находку M1–M6, L3, L4 и L6 — каждый с проверкой нагруженности откатом; они
перечислены в вердиктах `docs/records/2026-09-09[5]-post-wave-review.md`.
**Тесты идут с
включённым leak-трекером** —
`test/flutter_test_config.dart` зовёт `LeakTesting.enable()` на всю сьюту, и у
`example/scopo_demo` есть свой такой же.

**Тот же гейт гоняет машина** — `.github/workflows/ci.yml`, на `push` в `main`,
на любой pull request и по кнопке (`workflow_dispatch`). Шаги те же и в том же
порядке, плюс сьюта примера и `git status --porcelain` в конце (он строже
бывшего `git diff --exit-code`: замечает и созданный неотслеживаемый файл).
Рядом с `gate` стоит вторая job, `ceiling`, на текущем `channel: stable` и с
`continue-on-error`. Локальный прогон это не отменяет: CI отвечает через
несколько минут и на чистом клоне, а `docs/backlog.md` с правкой владельца в
дереве ему не мешает — pubignore-нутые файлы `pub publish --dry-run` не считает.

Восемь команд смотрят на `lib/`, `test/`, `example/*`, `README.md`, `doc/`,
`docs/ru/` и корневые файлы пакета. Коммит, который не тронул ничего из этого —
а `docs/handoff.md`, `docs/backlog.md`, `docs/records/` и `AGENTS.md` сюда не
входят, — гейта не касается вовсе, и пересчитывать из-за него нечего. **Тронул
что-то из списка — гейт надо прогнать заново**, даже если это одна строка в
`README.md`.

**Ни одна находка ревью гейтом не ловится, и это проверено четырежды.** Второе
ревью проходило на `9296648`, третье — на `72bd596`, четвёртое — на голове
`main` того дня; 43, 44 и 52 находки получены на зелёном гейте. Это стоит
помнить, решая, что считать достаточной проверкой.

| проверка | результат |
| --- | --- |
| `fvm flutter test` | **534 теста, все зелёные, leak-трекер включён** |
| `fvm flutter analyze` (корень) | `No issues found!` |
| `analyze` в обоих `example/*` | `No issues found!` в каждом |
| `fvm dart format --set-exit-if-changed lib test` | 115 файлов, 0 changed |
| `fvm dart doc --dry-run` | 0 warnings, 0 errors |
| `fvm dart pub publish --dry-run` | 0 warnings |
| `sh docs/ru/check.sh` | переводы актуальны: 16 |

Архив пакета — **276 КБ** сжатыми, измерено 2026-09-09 тем же
`publish --dry-run`; до правок по послеволновому ревью было 272 КБ, на 0.13.0 —
257 КБ (2026-08-31), на 0.12.0 — 233 КБ. Сьюта в
него не входит с волны 24 (L17):
756 КБ против 540 КБ у `lib/`, качает их каждый, кто зависит от пакета, а
документация — это темы в `doc/`, и они едут. **Сьюта примера — едет, и это
решение** (2026-08-31): строка `/test/` привязана к корню, обе половины довода
к девяти килобайтам образца не относятся, причина стоит в самом `.pubignore`.

**В гейт §6 не входит сьюта примера** — 5 тестов в `example/scopo_demo`,
`fvm flutter test` из его папки. **Тронул `lib/` — прогони и её.** Один тест
демо импортирует папку демо по пути, так что переименования его задевают. CI её
гоняет, но локально это по-прежнему на тебе. Вторая сьюта примеров (17 тестов
уроков) уехала вместе с `NavigationNode` в свой репозиторий, и гейт там свой.

Форматирование примеров в гейт тоже не входит, но `example/scopo_demo` с
форматтером сходится: `fvm dart format --set-exit-if-changed lib test` из его
папки даёт 0 changed.

**Бампнул версию в `pubspec.yaml` — обнови локи примеров.** Наступил на это
2026-08-23 при 0.10.0 → 0.11.0: примеры зависят от пакета по пути, и номер
версии стоит в их `pubspec.lock`. Локально гейт молчит (он их не пересобирает),
а CI делает `pub get` и падает на шаге «Nothing drifted» — он видит изменённые
локи (их два: `example/minimal` и `example/scopo_demo`). Лечится
`fvm flutter pub get` в каждом примере **закреплённым тулчейном** (чужой SDK
перерешает локи целиком, а не одну строку) и коммитом локов вместе с бампом.
Проверять так: `git diff example/*/pubspec.lock` должен показывать ровно
`version:` пакета и ничего больше.

Три ловушки самого гейта:

- **`pub publish --dry-run` считает предупреждением любой изменённый
  отслеживаемый файл** («2 checked-in files are modified in git»), так что на
  грязном дереве он даёт 1 warning и гейт «падает» на ровном месте. Гоняй его
  **после коммита**, а не до: на чистом дереве те же правки дают 0 warnings.
  Проверено 2026-09-09. Незатреканные и pubignore-нутые файлы он не считает —
  отсюда и путаница;
- `dart doc` и `pub publish` запускать **через `fvm dart`**, а не прямым бинарём
  из `~/fvm/versions/...`: без обёртки `dart doc` падает на
  `type 'Null' is not a subtype of type 'String'` в `DartdocOptionContext.sdkDir`;
- `dart format --set-exit-if-changed` **и пишет, и возвращает код**. Первый
  прогон после правок может вернуть 1, отформатировав файл; повторный даёт 0.
  Это не отказ гейта, но проверять надо повторным прогоном. **И гонять надо
  ровно команду из §6, без `--output=none`:** с этим ключом форматтер ничего не
  пишет, поэтому «1 changed» не уходит ни на втором прогоне, ни на десятом, а
  выглядит это как заевший гейт. Наступил 2026-09-09, коммит с неотформатированным
  тестом пришлось править `--amend`.

Тот же набор проходил и на прежних полах: на 3.29.0, пока он был полом, и на
глобальном 3.44.9 до него.

## Грабли, на которые не стоит наступать второй раз

**`analyze` до `dart format` — это не проверка (2026-09-09).** Гейт §6
перечисляет `analyze` раньше формата, и если гнать его списком сверху вниз, то
форматтер правит код **после** того, как анализатор его одобрил. Так уехал
красный `gate` на `eb8c799`: форматтер схлопнул `FlutterErrorDetails(...)` в
одну строку, и `require_trailing_commas` сработал на том, чего в прогоне
`analyze` ещё не было. Локально всё было зелено, потому что после `format`
повторного `analyze` не было. Правило простое: **последней командой перед
коммитом идёт `analyze`, а не `format`** — либо, если формат что-то изменил,
весь гейт гоняется заново. Прежняя запись про красный `gate` (`2243baf`,
2026-08-31) — про другое: там вывод не прочитали вовсе.

**`unawaited(…)` — не только про линт: он задаёт контекстный тип (2026-09-05).**
Обёртка сообщает выражению тип `Future<void>?`, и от этого зависит вывод
дженерика внутри. `element.close().then((_) => flag = true, onError: (e) {…})`
без неё выводит `R = bool` по стрелочному колбэку — и `onError`, ничего не
возвращающий, роняет тест на `ArgumentError: The error handler of Future.then
must return a value of the returned future's type`. Анализатор молчит, падает
только прогон. Найдено при снятии `unawaited` пачкой: пять тестов покраснели
разом, все в местах с `then(..., onError:)`. Правило: **снимаешь `unawaited` —
смотри, нет ли внутри `onError`**, и в любом случае прогоняй сьюту, а не только
`analyze`. Записано и в скилл `dart` («unawaited задаёт контекстный тип а не
только гасит линт», с прогоном на 3.6.0 и 3.13.0) — там формулировка общая, не
привязанная к этому репозиторию.

**Бросок изнутри `onListen` не доходит вообще ни до кого (2026-08-31).**
`_mergeStreams` в `scope_dependency_group.dart` собирает потоки ветвей внутри
`controller.onListen` — то есть функция, которая эти потоки делает, зовётся
оттуда. Синхронный бросок там не идёт ни слушателю, ни в `FlutterError`:
контроллер не закрывается, и обход встаёт **навсегда, без ошибки, без выхода и
без конца**. Так вело себя всё, что чужая реализация бросала до возврата
потока, — одинаково на входе и на выходе, а соседняя sequential-группа при том
же входе отвечала правильно. Лечится охраной вокруг **вызова**
(`_ScopeDependencyConcurrent._guarded`), а не внутри `_mergeStreams`. Правило
шире: **место, где поток создают, должно быть под той же охраной, что и его
ошибки**, иначе одна беда даёт два разных исхода. Найдено пробой; все восемь
проверок §6 при этом зелены — сьюта такого входа не имела.

**Охранник `_disposalIsOver` нельзя раскрыть шаблоном, хотя соседний раскрыт
(2026-08-29).** В `_prepareDependencies` первая ветка `switch` — шаблон
`ScopeDependency(state: ScopeDependencyInitial())`, и она такой стала намеренно
(`7607286`). Вторая, с предикатом, так не переписывается: **у шаблонов
непрошедшая ветка проваливается в следующую, а предикат спрашивает у миксина
только флаг** `_isDisposalDone`. Наивный перевод в два шаблона роняет тест
«a second init() on a tree that holds nothing fails the same way» — ровно
регрессию P2-1, закрытую `4059c4c`. В коде этого предупреждения нет, только сам
предикат.

**`--` внутри XML-комментария ломает файл целиком, и гейт этого не видит
(2026-08-24).** XML запрещает `--` внутри `<!-- … -->`. Переписанная шапка
`ide/scopo-live-templates.xml` несла его трижды — редактор отверг **весь файл**,
и группа шаблонов не появилась в настройках вовсе, без единого сообщения. Все
восемь команд §6 были при этом зелёные, потому что **ни одна из них не разбирает
этот файл как XML**: сьюта читает его текстом, `analyze` и `format` смотрят на
Dart. Правило шире одного файла: **редактор, отвергший конфиг, обычно молчит, а
не жалуется**, так что «поставил и не появилось» — это в первую очередь вопрос к
синтаксису файла, а не к содержимому. Сейчас сьюта держит два правила (`--` в
комментарии, голый `&`); полный разбор потребовал бы `package:xml` в
dev-зависимостях — не заводил, это решение владельца.

**`grep -r` по репозиторию читает `doc/api/` (2026-08-24).** Это каталог, куда
`dart doc` пишет по умолчанию: `.gitignore` и `.pubignore` прячут его от git и
от архива, но `grep -r` ходит по файловой системе и его видит. Проверка
«встречается ли имя снаружи `lib/`» дала из-за него 105–260 попаданий там, где
на самом деле ноль; сняло это только переключение на **`git grep`**. Каталог
при этом был мёртвый — 684 файла с именами до переименований 18 августа, — и
**удалён по просьбе владельца 2026-08-24**. Он вернётся с первым же настоящим
прогоном `dart doc` (гейт гоняет `--dry-run`, тот не пишет ничего): гоняешь
дартдок ради проверки — бери `--output` во временный каталог. Не путать с
`doc/*.md` — те под контролем версий и едут в архив.

**Пакет внутри пакета не разводится по архивам (2026-08-24).**
`dart pub publish` ставит начало обхода ignore-файлов в **корень
git-репозитория**, а не в папку публикуемого пакета. Строка `/packages/` в
корневом `.pubignore`, которой второй пакет убирался из архива scopo, прятала
его и от его собственной публикации: «The pubspec is hidden, probably by
.gitignore or pubignore», архив «<1 KB». Обход через `/packages/**` плюс `!/**`
в его собственном `.pubignore` тоже не работает — по семантике gitignore из
исключённого каталога вернуть нельзя ничего. Обе пробы записаны в
`docs/records/2026-08-24[3]-navigation-node-extraction-report.md`; отсюда у
`navigation_node` отдельный репозиторий, а не папка в этом.

**Ловушки тестов, найденные 2026-08-20.** Все четыре стоили времени, и все
четыре повторимы:

- **Дерево нельзя разворачивать целиком** (`pumpWidget(const SizedBox())`) в
  тесте, где разбор бросает или где родитель бросил ребёнка: отказ вылетает из
  `Element.unmount()`, обход фреймворка на этом кончается, и leak-трекер ловит
  неутилизированный `_UbiquitousInheritedElement` или
  `SingleChildRenderObjectElement`. Забирать надо только сам скоуп, оставив
  `Directionality` над ним, — так делает соседний существующий тест.
- **`hang.complete()` в теле теста конфликтует с
  `addTearDown(hang.complete)`** — `Bad state: Future already completed`. В
  `addTearDown` нужен `if (!hang.isCompleted)`.
- **Тест, который ждёт, что чего-то НЕ произойдёт, должен снимать предел, а не
  надеяться на скорость.** Идиома `settle(tester, until: () => false)` даёт
  честный шанс, но если рядом висит невыключенный таймаут по умолчанию, упадёт
  соседнее ожидание, а не проверяемое.
- **События single-subscription `StreamController`, созданного в теле
  widget-теста, не доставляются одним `pumpAndSettle`** — нужен
  `tester.runAsync(...)`. Broadcast-контроллер и `async*` доставляются
  нормально. На этом ревьюер чуть не выписал фантомный дефект.

Найдено по ходу волн, стоило времени, в коде не видно:

- **внешний `catch` в `_performAsyncInit` выполняется внутри
  `performRebuild`.** Отказы, ради которых он существует (поиск координатора,
  не-генераторный `initScope`), происходят **до первого `await`**, поэтому
  прямой `_model.update` оттуда дёргает `markNeedsBuild` посреди сборки
  элемента. Применять состояние только через
  `SchedulerBinding.instance.runOutsideFrame`. И `_initCompleter` завершать
  раньше всего, что может бросить: иначе `_performAsyncDispose` паркуется на
  нём навсегда, и сьюта не падает, а **висит** (волна 10);
- **ошибка внутри `yield*`-делегированного потока идёт прямо подписчику** —
  `try/catch` в генераторе её не видит, нужен явный `await for`;
- **ошибка, проброшенная из `_performAsyncDispose()`, до
  `tester.takeException()` не доходит**: метод вызывается на отброшенном
  future, поэтому такие тесты оборачивают в `runZonedGuarded`;
- **ассерт в `didUpdateWidget` рвёт кадр**: обновление бросается на середине и
  следом прилетает вторичный ассерт фреймворка (`'_dependents.isEmpty'`).
  Ставить такие проверки в `build`, где их ловит build error boundary;
- **у того же броска есть второе последствие: он утекает поддерево.** Бросок из
  **обновления** оставляет сломанное поддерево неразмонтированным навсегда —
  виджетов в дереве уже нет, вторичного исключения нет, а элементы и
  render-объекты ниже разрыва не разбираются никогда, и убирание дерева до них
  не доходит. Бросок из `build` так не делает: там дерево размонтируется как
  обычно. Проверено пробой на двадцати строках чистого Flutter, без scopo
  (волна 14). Отсюда девять тестов сьюты, у которых стоит
  `experimentalLeakTesting: unmountableTree`;
- **`close()` снимает скриншот поддерева**, поэтому фикстура для теста с
  `close()` должна иметь ненулевой размер, а не `SizedBox.shrink()`. И по той
  же причине **вложенный скоуп не удерживает родителя через `close()`**:
  `ScreenshotReplacer` заменяет поддерево картинкой, ребёнок размонтируется и
  снимается с учёта. Чтобы ожидание детей истекло, у ребёнка должен зависнуть
  собственный разбор (волна 11);
- **три копии `_settle` в тестах ведут себя по-разному.** Общий
  `test/utils/settle.dart` двигает и реальное, и фейковое время
  (`runAsync` + `pump(step)`), а приватная копия в `lite_scope_test.dart`
  делает `pump()` без длительности — фейковые часы стоят, и таймер зоны
  (`Future.timeout` внутри `waitForChildren`) не срабатывает никогда. Тест на
  такой таймер надо писать на общем помощнике (волна 11);
- **таймер, заведённый на разборе, роняет чужие виджет-тесты.** Лимит через
  `Future.timeout` заводит таймер зоны сразу, а под тестовым биндингом отмена
  подписки не завершается вовсе — таймер оставался непогашенным к концу любого
  теста с живым скоупом (`A Timer is still pending`, 17 падений). Лимиты на
  такие ожидания брать из корневой зоны: `Zone.root.createTimer` (волна 7);
- **у Flutter нет хука «зависимый начинает сборку»**: `Element.dirty` во время
  `build()` всегда `true`, идентичность `widget` не ловит `setState`, у
  `InheritedElement` подходящего хука нет. Отсюда граница сборки по кадру в
  `scope_widget_core.dart`;
- **`git checkout -- <файл>` не отменяет мутацию, а стирает по этому файлу всю
  несохранённую работу.** Волна 12 потеряла так `hasData` и охрану второго
  `ready` в `async_data_scope_core.dart`. Для мутаций годится
  `git stash push -- <файл>` и `git stash pop`; мутацию, внесённую скриптом,
  снимать обратным скриптом. **Ещё надёжнее — вообще не мутировать рабочее
  дерево:** третье ревью гоняло 19 мутаций и 12 проб в копии репозитория,
  распакованной за пределами проекта (`tar` без `.git`, `build`, `docs`).
  Копия видит тот же `fvm`, `pub get` не нужен, сьюта на ней зелёная сразу.
  Так рабочее дерево остаётся чистым, а мутацию не надо снимать вручную;
- **ветка `_debugIsDescendantOf` в `markNeedsBuild` — разрешающая, а не
  бросающая.** `markNeedsBuild()` на элементе, который *сам сейчас строится*,
  ассертом Flutter **не ловится** (`return true` внутри `assert`), а дальше
  глотается через `if (dirty) return;`. Отсюда P1-2 третьего ревью. Не
  рассчитывай, что фреймворк предупредит;
- **своя подмена `FlutterError.onError` глотает провал собственного теста.**
  Тест, собирающий репорты в список на всё своё тело, ловит туда же и
  `TestFailure` от упавшего `expect`: прогон не краснеет, а **виснет**.
  Возвращать обработчик надо до проверок, а не только тир-дауном (волна 13);
- **прерванный `flutter test` оставляет живой `flutter_tester`**, и следующий
  прогон висит на нём, ничего не печатая. Снимать процесс руками (волна 13) —
  **но не `pkill -f flutter_tester` вслепую.** На этой машине параллельно идут
  прогоны других проектов (`~/development/tez/ttm-driver-speed`, SDK 3.44.4,
  запускается через `melos exec --concurrency 10`), и такой pkill рубит живой
  чужой прогон. Проверено дорого: 2026-08-17 я так и сделал — процессы выглядели
  зависшими, владелец подтвердил «хозяев не осталось», — а они появились снова с
  новыми pid, потому что `melos` был жив и запускал их заново. То есть **и
  «висит давно», и слово владельца не доказывают, что процесс бесхозный:
  доказывает только родитель.** Смотреть `ps -eo pid,ppid,etime,args`, найти
  ppid тестера и убедиться, что это не работающий раннер; свои узнаются по
  версии тулчейна (`3.27.0`; до 2026-08-18 это была `3.29.0`) и по `my/scopo` в
  аргументах, чужие — по `3.44.4` (волна 14). **Тот же зверь, но другая грань:
  второй свой же прогон `fvm flutter test` параллельно первому по тому же
  дереву не просто оставляет сироту — он портит результат.** Два процесса
  делят один `.dart_tool`/`build`, и от гонки изолятов за общий кеш сборки
  прогон может показать событие, которого код заведомо не производит (так
  простой `LiteScope` без своего хука отчитался `onReady`). И отдельно: PID,
  который харнесс отдаёт как результат запуска, — это обёртка
  (`flutter_tools.snapshot test`), а не сам движок; она заводит отдельный
  `flutter_tester` на каждый тестовый файл, и убийство только внешнего PID
  сироту не снимает. Снимать нужно всё поддерево процессов — тем же
  `ps -eo pid,ppid,etime,args` — и следом `fvm flutter clean && fvm flutter
  pub get`, прежде чем доверять следующему прогону (задача 2 плана
  наблюдателя, 2026-08-19);
- **`IconButton.tooltip` съедает длинное нажатие, повешенное снаружи.**
  `IconButton` строит `Tooltip` **внутри себя**, над своим `InkWell`, а у
  `Tooltip` есть собственный `LongPressGestureRecognizer`. Обёртка
  `GestureDetector(onLongPress:)` снаружи кнопки оказывается выше него, её
  таймер стартует позже, арену забирает подсказка — и внешний обработчик не
  зовётся ни разу. Лечится подъёмом подсказки: `Tooltip` → `GestureDetector` →
  `IconButton` без `tooltip:`. Поймано тестом при переносе с
  `IconButton.onLongPress` на 3.27 — глазами в коде этого не видно, а гейт
  сьюты примеров не гоняет (2026-08-18);
- **публикатор логов в тесте пишется через `implements`, и только так.**
  `CustomLogPublisher` — `abstract interface class`, наследовать его нельзя
  (`extends` даёт ошибку компиляции). Фабрика `ScopeLogPublisher(fn)` тоже не
  выручает: в `const`-контексте анализатор теряет аргумент типа и говорит
  `const_constructor_param_type_mismatch`, а без `const` требует его обратно
  через `prefer_const_constructors`. Рабочий вид — свой `final class … implements
  ScopeLogPublisher` с `const`-конструктором (`test/scope_logger_test.dart`,
  2026-08-19);
- **бросок из `onPopInvokedWithResult` оставляет навигатор запертым.**
  Пользовательский колбэк, который роняет на `didPop: true`, бросает внутри
  `NavigatorState._flushHistoryUpdates`, а `_debugLocked` снимается ассертом в
  конце `pop()` — до него дело не доходит. Дальше любой разбор дерева даёт
  вторичный `'!_debugLocked'` из `NavigatorState.dispose`, так что тест краснеет
  не тем, чем задумывался. Отказ для теста ставить туда, где фреймворк ещё цел
  (волна 15);
- **корневой `flutter analyze` требует, чтобы примеры были фетчены.** Он
  обходит всё дерево, `example/` включая, и на чистом клоне даёт 60 ошибок про
  несуществующие `package:`-URI: пакеты примеров не разрешены. Локально шаг
  проходит только потому, что `pub get` в примерах когда-то делали. То есть
  локальный гейт годами держался на необъявленном состоянии рабочего дерева, и
  показала это первая же попытка прогона CI (волна 15). Порядок в CI:
  `pub get` примеров — **до** корневого анализа;
- **`stamp.sh` штампует и то, чего не правили.** Правка русского зеркала
  прошла, парная правка оригинала — нет (команда упала на разборе шелла), и
  `stamp.sh` записал в перевод хеш неизменившегося оригинала; `check.sh` после
  этого доволен. Смотреть `git status` **до** штампа (волна 12);
- **каскад на классе не работает:** `ScopeConfig..field = value` — ошибка
  компиляции, у `Type` нет сеттеров;
- **каскад в теле стрелки прилипает к выражению, а не к получателю:**
  `dep..unmount = () => log.add(x)..dispose = …` разбирается как
  `log.add(x)..dispose = …`, то есть каскад уходит внутрь лямбды. Ошибка
  компиляции невнятная («The setter 'dispose' isn't defined for the class
  'void'»). Лечится блочным телом или отдельными присваиваниями (волна 14);
- под `AutomatedTestWidgetsFlutterBinding` ошибка, лежащая в буфере потока до
  подписки (`Stream.error`), не проходит через `asyncMap` — а именно так
  подписывается `_performAsyncInit`. Ветку ошибки поднимать `async*`-потоком,
  который бросает после первого события; `Stream.error` — только внутри
  `tester.runAsync`. Разбор:
  `docs/records/2026-08-12[1]-stream-error-under-test-binding-report.md`;
- **два прогона `fvm flutter test` зависли без вывода при работе над
  `ScopeConfig.observer`.** План `2026-08-19[2]-scope-observer-plan.md`,
  задача 1: наблюдатель, чей
  `onInit` реентерно зовёт `tester.pumpWidget` из уже строящегося дерева, и
  отдельная проба, подменявшая `FlutterError.reportError` на `print` внутри
  `notifyObserver`. Оба раза `flutter test` не падал и ничего не печатал —
  прогон снимали руками (`TaskStop`). Причина не установлена; в обоих сценариях
  тест держал свою подмену `FlutterError.onError`, как и в грабле про волну 13
  выше — тот ли это механизм, не проверено. Закоммиченный код и тесты задачи 1
  не висят; разбор — `task-1-report.md` в каталоге плана.

## Известные проблемы

### Решения, которые стоит помнить

Списки **всех** ревью закрыты, у каждой находки вердикт в своём файле —
последним 2026-09-09 закрылось послеволновое, 22 находки. Ниже —
не бухгалтерия, а то, что из закрытого действует дальше и что не стоит
открывать заново; счёт по спискам и чем какая волна кончилась — в
`docs/records/2026-08-30[1]-handoff-history-log.md`.

Три вещи из четвёртого ревью закрыты не правкой, а разбором, и их стоит знать,
чтобы не открывать заново: **M15** — пропавший `scheduleFrame()` виджет-тесту
невидим, потому что `pump()` рисует кадр безусловно; **M18** и релизная
половина **M5** — за ассертом, а `flutter test` всегда с ассертами; **M12** —
две взаимно-избыточные охраны, каждая делает другую недостижимой, так что
убить тестом можно любую, но не обе. У всех трёх причина записана в коде.

Что из волны 18 стоит помнить как решение: **`NavigationNode.enabled`** —
приложение говорит узлу, что он не тот, кто на экране; сам узел этого узнать не
может. И **`AsyncScopeElementBase.boundsDisposeScopeItself`** — семейство с
двумя стадиями за `disposeScope()` ограничивает их само, потому что один лимит
вокруг обеих пропускает вторую.

Что из волны 17 стоит помнить как решение: **отказ, у которого не осталось
адресата, отчитывается и не бросается** — это и раньше было правилом четырёх
стадий разбора, а теперь оно же действует у `unmount()` элемента и у
`ScopeController`. Броском отказ уходит только туда, где вызывающий есть:
`close()` слышит, фреймворк — нет.

Что из волны 16 стоит помнить как решение: **`ScopeTimeout.none` значит одно и
то же везде, где его пишут**, включая все четыре умолчания `ScopeConfig`, — а
там, где «ждать сколько понадобится» смысла не имеет (`initCancellationTimeout`
у отдельного скоупа, `pauseAfterInitialization` у любого), он отвергается
ассертом, а не наделяется вторым смыслом. И **отрицательный `Duration` теперь
отказ, а не мгновенно истекающий предел** — это цена того, что маркер узнают по
типу: арифметика его теряет, и потерянный он раньше значил ровно обратное.

Что из закрытого стоит помнить как решение, а не как правку:

- **подписаться на скоуп можно только из сборки** — теперь это ассерт в
  `ScopeContext._find` и правило в `doc/base.md`. Граница пересборки взята по
  кадру, и подписка из `didChangeDependencies` жила ровно до первой
  пересборки от родителя;
- **`value` у `.value`-конструкторов не-nullable** — `null` перестал
  компилироваться, ассерт для этого не понадобился;
- **`asStream` берёт один аргумент типа вместо двух** — тип контейнера
  выводится, приведение `this as T` исчезло;
- **инварианты семейств запечатаны** (`@nonVirtual` на `initScope`
  data-слоя и `initDataAsync` контроллерного, `@mustCallSuper` на его
  `disposeScope`) — гарантия анализаторная, тестами не покрывается;
- **`data` отвечает раньше, чем модель становится ready, и так и задумано.**
  Окно между `_hasData` и `AsyncScopeReady` закрывать нельзя: `disposeScope`
  читает `data` именно в нём, и обещание «`dispose` всегда получает значение»
  держится на этом. Исправлено обещание, а не код (P3-7);
- **`isBuilding` спрашивает три источника, и только один из них ассертный.**
  `buildScope` бывает и вне кадра — так строит первое дерево `runApp`, — а флаг
  build owner'а живёт внутри `assert`, поэтому в release от него ничего не
  остаётся. Это была P2-7, **закрытая волной 14**: пакет считает свои
  пересборки обычным полем (`_scopeRebuildDepth` в `is_building.dart`), и
  `isBuilding` спрашивает его тоже. Не-ассертного признака чужой сборки у
  фреймворка нет вовсе, так что в release неотвеченной остаётся сборка, не
  принадлежащая ни кадру, ни пакету; это сказано в дартдоке и в теме `utils`;
- **три метода `ScopeController` — односторонняя последовательность**:
  `performInit` после `performDispose` ничего не делает, а разбор один на
  контроллер и его исход видят все вызвавшие;
- **`ScopeConfig.reset()`** возвращает пять глобальных переключателей к
  умолчаниям; наблюдателя намеренно не трогает — раньше по той же причине не
  трогал логгер. Три сьюты пакета им пользуются;
- **у элемента на асинхронной ветке нет достижимого пути потерять
  собственный `disposed`.** `init()` синхронный и идёт одним стеком с
  `mount() → build()`, без точки, где `unmount()` мог бы вклиниться раньше
  конца; провал `init()` ставит `_initPhase` в `failed`, не оставляет в
  `pending`, а error boundary Flutter гасит его на месте потомка, наружу
  через `mount()` родителя не пробрасывая. `unmount()` зовёт `dispose()` (а
  с ним и единственный источник `onDisposed`) только когда `_initPhase` уже
  не `pending` — то есть всегда. Проверено трассировкой кода, реализуемого
  сценария потери не нашлось; записано как закрытый вопрос, а не как
  опасение — с подавленной для этой ветки базовой парой
  `ScopeWidgetElementBase.reportsOwnLifecycle`, это и есть то, что держит
  `disposed` от потери (задача 2 плана наблюдателя).

### Предмержевое ревью ветки `observer`

Последняя проверка перед мержем: пять находок, все закрыты 2026-08-19. Две из
них были и записями «Открыто, вне списка ревью» — вычеркнуты там же. Что стоит
унести дальше:

- **метка объекта живёт ровно столько, сколько живёт виджет, а хуки бывают
  позже.** `debugLabel` асинхронного элемента читал `widget`, а разбор отдаёт
  виджет в своём `finally` (`_widget = null`, и `Element._widget` к этому
  моменту тоже `null`). Поэтому «брошенное ожидание, упавшее потом»
  (`ScopePhase.abandonedWait`) приходило наблюдателю **как отказ самого
  наблюдателя**: `debugLabel` бросал `TypeError`, защита в `notifyObserver`
  ловила его и репортила про наблюдателя, а настоящий отказ не доходил ни до
  кого. Лечится кешем метки, снятым рядом с `_widget = widget` в `dispose()`.
  Тот же класс граблей уже был записан в контроллерном семействе — там через
  `widget` читали `disposeScopeTimeout`. **Правило: всё, что зовут после
  `_disposalFinished`, не имеет права трогать `widget`;**
- **`ScopePhase.unmount` достижима только через `close()`.**
  `ScopeWidgetElementBase.unmount()` зовёт `unmountScope()` **до** `dispose()`,
  так что у скоупа, снятого с дерева, первая стадия разбора уже пуста, и
  упасть в ней нечему. Тест на эту фазу пишется на `LiteScopeCore` с
  `close()`, а не на `AsyncScope`;
- **отмена в очереди за `scopeKey` не даёт `onInit` вовсе.** Не «`onCancelled`
  раньше `onInit`», как думало ревью, а `onCancelled` в одиночку:
  `_performAsyncInit` возвращается сразу после него, до подписки. Запись
  скоупа целиком — `cancelled`, `dispose`, `disposed`. Записано в теме `debug`
  и в дартдоке `onCancelled`;
- **`RecordingObserver` из `test/utils/observer.dart` теперь оставляет `tag` в
  метке** и срезает только `(#hash)`. Без этого родителя и ребёнка одного типа
  в записи не различить, а тест на ожидание детей без этого не написать;
- **`ScopeAutoDependencies` шлёт `unmount` и `abandonedWait` тоже** —
  `scope_auto_dependency.dart:142` и `:218`. То есть «фаза недостижима» надо
  проверять по всем отправителям, а не только по `async_scope_core.dart`.

### Открыто, вне списка ревью

Четыре записи. Три — принятые границы с записанной причиной; четвёртая, первая
в списке, — долг, и она ждёт решения владельца. **Три записи закрыты
2026-09-01** — `7985fb6`, `47bbb95` и `66b1745`; все три были не границами, а
долгом, оставленным по §8, и разбор их уехал в `CHANGELOG` и сообщения
коммитов. Последняя из трёх нашлась при закрытии предыдущей и прожила в этом
разделе один коммит. Закрытые записи этого раздела уехали в
`docs/records/2026-08-30[1]-handoff-history-log.md`.

- **`ctx.wait` в документах и примерах стоит не по назначению; найдено
  2026-09-05 владельцем по соседнему пакету `solo`.** Там у ожидания появился
  третий член — `JobContext.join` (`solo`, `7b84552`, разбор в его
  `docs/records/2026-09-04[4]-join-design.md`): `wait` отцепляется от действия
  и бросает сразу, `join` дожидается действия целиком и бросает после него,
  `uncancellable` отмену не пускает вовсе. У нас член один, и им написаны все
  примеры — включая те, что учат не терять взятое. Дефекта два.
  **Первый: вызов бросают на полпути, и `catch` тела освобождает то, что ещё
  работает** — `database.migrate` под `database.close()`
  (`doc/async_data_scope.md:70` и `:84`), `analytics.init` под
  `analytics.dispose()` (`example/scopo_demo/lib/app/app_dependencies.dart:49`,
  и там же `:53`, `:57`), `Session.restore` под `storage.close()`
  (`doc/full_scope.md:289`), `opened.authenticate` под `opened.close()`
  (`doc/async_scope.md:251`). **Второй: отмена во время приобретения теряет сам
  ресурс** — значение до тела не доходит, освободить его некому:
  `ctx.wait(Api.connect)` в **правильном** примере `doc/async_scope.md:248`,
  `ctx.wait(connection.open)` в `README.md:219`, `Storage.open` в
  `doc/full_scope.md:285`, `Database.open`/`Session.connect` в
  `doc/async_scope.md:286` и `:289`, `return ctx.wait(Database.open)` в
  `doc/async_data_scope.md:20` и `README.md:246`, и то же в шаблонах IDE
  (`ide/scopo.code-snippets:189`, `:193`, `:581`).

  **Третий член семьи для этого не нужен, и решение владельца от 2026-09-05 —
  не оборачивать вовсе: `initData: (context, ctx) => Database.open()`, а
  обязанность дождаться лежит на том, кто запустил инициализацию.** Движок это
  уже делает, и это уже под тестом: `runInitBody` каждого семейства ловит
  значение, произведённое после отмены, и отдаёт его релизу —
  `disposeScope()` у `AsyncScope` (`async_scope_core.dart:294`),
  `releaseLateData` → `disposeData` у `AsyncDataScope`
  (`async_data_scope_core.dart:117`), `releaseLateDependencies` → `onUnmount` +
  `dispose` у `Scope` (`full_scope/scope_core.dart:175`), свой релиз на всех
  путях у `AsyncControllerScope`. Тест — «is not thrown into when it waits on a
  bare await, and the scope releases what it produced instead»
  (`scope_removed_while_initializing_test.dart:173`), прогнан 2026-09-05,
  зелёный. То есть `ctx.wait` вокруг приобретения **отключает** собственную
  сеть движка: она ловит только то, что тело **вернуло**.

  **Одна оговорка, и она ждёт решения владельца:** сеть работает, пока разбор
  ещё ждёт — `canReleaseAfterCancellation => !_disposalFinished`
  (`async_scope_core.dart:324`), а ждёт он `initCancellationTimeout`, по
  умолчанию **3 секунды** (`ScopeConfig._timeout`). Тело, не уложившееся в них,
  отдаёт построенное в никуда, и `false` там проходится молчанием намеренно.
  Значит «запустивший обязан дождаться» сегодня верно только внутри лимита.

  **Спека — `docs/records/2026-09-05[1]-init-wait-family-design.md`:** правило
  целиком, разбор обоих дефектов с координатами, список мест под правку и
  порядок работ. **Половина, не зависящая от `Job`, сделана 2026-09-05,
  `5364476`:** приобретения зовут напрямую во всех темах, обоих `README`, пяти
  зеркалах, двух примерах и шаблонах редакторов; правило записано в тему
  `AsyncScope` и в дартдок, запись добавлена в `CHANGELOG.md` 0.14.0. Гейт §6
  пройден целиком, плюс сьюта демки. Первый дефект при этом закрылся без
  `join`: шаги зовут напрямую, а отмена доходит следующим `ctx.progress`.

  **Остальное отложено решением владельца 2026-09-05: ждём публикации `solo`,
  который готовит версию своей `Job` для scopo** — она может изменить
  постановку целиком, поэтому два решения из спеки (брать ли `join`; что
  делать с `initCancellationTimeout`, у которого умолчание 3 секунды) до тех
  пор не принимаются. Если `join` возьмут, правленные места меняются
  механически: `await x(); ctx.check();` → `await ctx.join(x)`. **Что у соседа
  с тех пор изменилось** — ядро вынесено в отдельный пакет `jobs`, семейство
  ожидания сменило форму, имя пакета не окончательное — сверено 2026-09-08 и
  записано в «Что дальше».
- **Из четырёх ожиданий разбора ограничены три; четвёртое — нет, и так
  решено.** Ожидание появилось волной 8, лимита у него нет, и **2026-08-22
  владелец решил лимит не заводить**: вопросом эта запись быть перестала. Ниже
  — разбор, которым решение подпёрто; он получен запуском, кода не тронул, и
  прежнюю формулировку («завершают completer сам разбор и колбэки подписки, то
  есть код пакета, а не пользователя, и сценария зависания не нашлось»)
  поправляет в двух местах.

  - **таких мест не одно, а два.** `await _initCompleter.future` стоит в слое
    скоупа (`async_scope_core.dart`, шаг «инициализацию дожидаются, если
    отменить не удалось») и в слое состояния (`lite_scope_core.dart`, разбор
    состояния ждёт собственную `initStateAsync`). Прежняя запись называла
    одно, а говорила про оба — и «код пакета, а не пользователя» верно только
    для первого: второй завершает пользовательская `initStateAsync`;
  - **до первого доходят ровно одним путём, и он достижим.** У скоупа есть
    `scopeKey`, он встал за ним в очередь, и его снимают с дерева, пока
    очередь не подошла. Единственный `await` до создания `_subscription` —
    `coordinator.enter`; во всех прочих случаях подписка уже есть, и её отмена
    завершает completer строкой выше. Проба даёт
    `cancel waiting for access` → `wait for initialization` →
    `access cancelled` → `cancelled` → `disposed` → `exit from [scopeKey]`;
  - **будит его `entry.cancel()` строкой выше, и лимит здесь был бы лишним:**
    ждать нечего, кроме одного микротаска. Отмена завершает
    `coordinator.enter`, инициализация возобновляется, видит `_isDisposing`,
    шлёт `onCancelled` и завершает completer сама;
  - **связка нагружена.** Мутация «убрать `entry.cancel()`» останавливает
    разбор ровно на `wait for initialization`: ни `disposed`, ни выхода из
    очереди — скоуп навсегда держит ключ, и родитель ждёт его весь свой
    `waitForChildrenTimeout`. Роняет два теста `scope_observer_test.dart` —
    «a scope cancelled while queued for its scopeKey reports no onInit at all»
    и «a hanging teardown reports onTimeout after disposeScopeTimeout» — плюс
    leak-трекер;
  - **второе место завершает пользовательский код, но ограничено снаружи** —
    `disposeScopeTimeout`. Проба: состояние, чья `initStateAsync` не
    завершается никогда, снятое с дерева, даёт `timeout … its own teardown` и
    следом `disposed`. То есть неограниченное оно только на своей строке, а не
    по последствиям, — и это причина не заводить лимит и здесь;
  - ~~**зато этот путь не закрыт тестом, и это уже пробел, а не граница.**~~
    **Закрыт 2026-08-23**, двумя тестами: `Scope` —
    «disposes of its dependencies after a state still initializing»
    (`cleanup_after_user_error_test.dart`, рядом с тестом волны 18 на висящий
    `disposeStateAsync`), `LiteScope` —
    «gives up on a state that never finishes initializing»
    (`scope_removed_while_initializing_test.dart`, третья группа). Оба
    нагружены откатом: снятие лимита вокруг половины состояния валит первый
    (`TimeoutException` не приходит вовсе), `boundsDisposeScopeItself => true`
    у базового класса валит второй (разбор `LiteScope` виснет на
    `dispose`, без `timeout` и без `disposed`). Тестов 451 → **453**.
- **`AsyncControllerScopeElementBase` читает приватное `_initSucceeded`**
  соседнего слоя (`AsyncScopeElementBase`). Внутри одной библиотеки законно и
  сделано намеренно: это тот же признак, по которому `_performAsyncDispose`
  решает, звать ли `disposeScope`, и разъехаться они не могут. Связь неявная,
  поэтому закрыта комментарием в коде и мутацией в тестах.
- **Зависимый, перестроенный дважды за один кадр, сохраняет оба набора
  селекторов** — граница сборки взята по кадру, и этот случай она не различает.
  Цена — одна лишняя сборка. Записано в `doc/scope_widget.md`.

### Тулчейн и окружение

- **`scopo` наследует два не-`base` класса ядра под `^0.1.0`, и это риск
  апгрейда** (записано 2026-09-09 по решению владельца — оставить как есть).
  `ScopeInitJob<T> extends JobBase<T>`, а контекст — `JobContextBase`; ни то
  ни другое в `lib/scopo.dart` не перечислено, так что `@protected`-члены ядра
  видны на публичном типе `ScopeInitJob`, и от вызова снаружи их держит
  предупреждение анализатора, а не ошибка. Сегодня это ничего не ломает.
  **Ломающим станет апгрейд ядра внутри `0.1.x`:** добавление абстрактного
  `@protected`-члена не ломает того, кто пользуется интерфейсами, и ломает
  наследника — а диапазон `^0.1.0` это разрешает. Поднимаешь версию
  `async_job` — сперва посмотри, не появилось ли у `JobBase` или
  `JobContextBase` новых абстрактных членов. Разбор — в вердикте хвоста Info
  `docs/records/2026-09-09[3]-bug-hunter-review.md`.
- **Codex на ревью требует трёх `--add-dir`, а не двух** (уточнено 2026-09-08).
  К `~/fvm` и `~/.pub-cache` добавился **`~/.dart-tool`**: Dart CLI трогает
  `dart-flutter-telemetry-session.json` на каждом запуске, и отказ песочницы
  валит команду целиком — `test`, `run`, хвост `analyze`, `pub publish
  --dry-run`. В рабочей памяти агента было записано, что этот отказ «снимается
  повтором»; это неверно и стоило целого прохода ревью в соседнем репозитории,
  где ни один тест так и не выполнился, притом каждая проверка повторялась
  дважды. Симптом обманчив: содержательная часть вывода выглядит выполненной
  (`No issues found!`), а последняя строка — `<asynchronous suspension>` и код
  возврата 1. Полный рецепт запуска, разбор симптома и замер — в скилле
  `~/.claude/skills/codex/SKILL.md`, раздел «Песочница»;
- **`discarded_futures` выключен 2026-09-05 решением владельца**
  (`analysis_options.yaml:75`, причина записана там же). Линт срабатывал на
  каждом future, запущенном из колбэка, который его и не мог бы дождаться, —
  post-frame callback, `builder`, тело `fakeAsync`, — и обёртка, которую он
  требовал, там ничего не сообщала. Случай, который важен, — future,
  оставленный без `await` **внутри async-тела**, — держит `unawaited_futures`,
  и он остался включён. Вместе с линтом убраны девять `unawaited`, стоявших
  только ради него: два в `screenshot_replacer.dart`, по одному в
  `async_scope_core.dart` и `scope_init_context.dart`, три в
  `scope_coordination_test.dart` и два в `lite_scope_test.dart`. Прочие
  шестьдесят с лишним остались: их требует `unawaited_futures`.

  Поводом был **красный `ceiling` на 0.14.0**: `flutter analyze` свежим
  стабильным тулчейном (3.47.2) дал `discarded_futures` там, где пол 3.27.0
  молчит. Прогон при этом был успешен — у задания `continue-on-error: true`.
  Всплыло впервые потому, что файл появился в 0.14.0, а её коммиты до
  2026-09-05 не были запушены: потолок видел этот код первый раз. Отсюда
  правило на будущее: **красный `ceiling` при зелёном `gate` — не сбой CI, а
  сообщение от потолка**, и смотреть его надо на каждом прогоне, где в `lib/`
  появился новый файл.

  Мелочь на потом: в `example/minimal/analysis_options.yaml:13` и
  `example/scopo_demo/analysis_options.yaml:13` стоит `discarded_futures:
  false` — теперь это дубль корневого решения. Не трогал (§8).
- **CI пишет предупреждение про Node.js 20 на каждом прогоне.**
  `actions/checkout@v4` (`.github/workflows/ci.yml:25`) заявляет Node 20,
  раннер принудительно даёт ему Node 24 и сообщает об этом аннотацией. Прогон
  зелёный, это не отказ. Лечится подъёмом до `actions/checkout@v5`. Не сделано
  намеренно: замечено 2026-08-22 по ходу другой работы, а чинить смежное заодно
  здесь не принято (`AGENTS.md` §8). `subosito/flutter-action@v2` рядом не
  жалуется.
- **Дев-зависимости запинены под пол, а не по вкусу.** `fake_async: ^1.3.1` и
  `leak_tracker_flutter_testing: ^3.0.8` — Flutter пинит и то и другое
  **точными** версиями, и у 3.27 это ровно они. Поднимешь до `^1.3.2` /
  `^3.0.9` — разъедешься с полом. На 3.29 те же ограничения дают 1.3.2 и 3.0.9,
  то есть цифры здесь про пол, а не про пакет.
- **`logger_builder` из пакета ушёл** 2026-08-19 вместе с журналом — его заменил
  `ScopeConfig.observer`, и в `pubspec.yaml` этой зависимости больше нет.
  Когда-то она держала пол Flutter (`logger_builder 0.5.0` требовал
  `meta ^1.16.0`, а Flutter пинит `meta` точной версией), и сцепка распалась
  дважды: сначала публикацией 0.6.1 с `meta ^1.15.0`, потом уходом самой
  зависимости. **Пол опущен до 3.27.0 решением владельца 2026-08-18** по замеру,
  а не по рассуждению — гейт прогнан на 3.27.0 в клоне за пределами рабочего
  дерева. Вся история и цена замера — в архивной записи.
- **`sdk: ^3.6.0` совпадает с полом ровно, и это проверяется каждым прогоном.**
  Flutter 3.27.0 несёт Dart 3.6.0 — ровно нижнюю границу `sdk`. До 2026-08-18
  граница не связывала ничего (пол 3.29.0 нёс Dart 3.7.0) и держалась ради
  **языковой версии**: на 3.6 `dart format` работает в старом стиле, на 3.7
  переключается на tall style. Измерено 2026-08-17 (поднять `sdk`, `pub get`,
  `dart format -o none --set-exit-if-changed`): **69 файлов из 94 переформатируются**.
  Форматтер берёт языковую версию из `.dart_tool/package_config.json`, поэтому
  проверять это без `pub get` бессмысленно — первая проба показала 0 changed и
  была неверной.

  **Подъём до `^3.7.0` отменён владельцем 2026-08-18** и удалён из бэклога.
  Тянул за собой снятие `require_trailing_commas`
  (`analysis_options.yaml:219`): tall style *убирает* запятые, которых требует
  правило. Вернётся он только вместе с полом 3.29. В `logger_builder` это уже
  прожито — 37 файлов из 45 и 43 замечания анализатора на дереве, которое
  `dart format` только что создал (`6128bc8`).
- Расхождения между версиями анализатора реальны: два замечания `analyze`,
  закрытые в `b697775`, видел только тулчейн 3.29, но не 3.44.9. Отсюда правило
  работать на поле.
- Flutter 3.29.0 с Xcode 26.5 печатает warning о двух совпавших macOS
  destinations. Косметика старого Flutter tools, release всё равно universal;
  upstream исправил в 3.35.0. Не подавлять. Разбор:
  `docs/records/2026-08-14[8]-xcode-destination-warning-report.md`.
- `flutter run -d macos --release` не завершается следом за закрытым
  приложением — открытый баг Flutter tools `flutter/flutter#58459`, не утечка
  scopo. Для проверки release собирать `fvm flutter build macos --release` и
  открывать `.app` напрямую. Разбор:
  `docs/records/2026-08-14[7]-release-run-hang-report.md`.

### В рабочем дереве

- **соседний репозиторий**: `~/development/my/navigation_node`,
  [vi-k/navigation_node](https://github.com/vi-k/navigation_node) — пакет,
  вынесенный по U7, **0.3.0 на pub.dev** (0.1.0 и 0.1.1 — 2026-08-24, затем
  0.2.0 и 0.3.0). Свой `AGENTS.md`, свой
  `docs/handoff.md`, свой CI, свой гейт из семи команд; тулчейн тот же, 3.27.0
  через fvm. `example/scopo_demo` зависит от него с pub.dev, поэтому правка
  узла доезжает до демки только через публикацию.
  Правило §5 про чужие правки действует и между репозиториями: не коммить в
  один то, что делалось для другого;
- дерево чистое. `docs/backlog.md` пуст и совпадает с `HEAD`: запись владельца
  от 2026-08-19 («стоит ли дальше использовать logger_builder») лежала
  незакоммиченной, работа по ней сделана, и удаление по §3 вернуло файл ровно
  к тому, что уже было в истории, — поэтому в диффе задачи 5 его нет вовсе;
- **`.vscode/scopo.code-snippets` — рабочая копия шаблонов**, заведена
  2026-08-23 по просьбе владельца, чтобы пробовать сниппеты прямо в этом
  репозитории, а не доставать их из pub-cache. Она **под контролем версий**
  (`.gitignore` исключает из `.vscode/` только `settings.json`) и **не едет в
  архив** (`.pubignore` вырезает весь `/.vscode/`). Единственный источник —
  `ide/scopo.code-snippets`; правишь его — копируй поверх. Расхождение краснит
  тест «the copy in .vscode matches the one that ships», проверено откатом;
- каталоги `.claude/` и `.superpowers/` — чужие, рабочие каталоги агентов. В
  `git status` они не показываются (исключены не через `.gitignore`, а глобально
  или в `.git/info/exclude`), но лежат в корне: не трогать и не добавлять в
  индекс;
- в `git stash` лежит чужой давний стеш `90ccbbc update README` — не трогать;
- корневой `pubspec.lock` в `.gitignore`; локи обоих примеров закоммичены и
  пересозданы закреплённым тулчейном.

## Пробелы в покрытии

**Сьюта измерена мутациями 2026-08-20: эффективный score ≈ 83 %** — 94
прицельные мутации, 70 пойманы, из 24 выживших около семи эквивалентны. Падает
она точно: обычно 1–3 теста, и имя теста дословно называет сломанный инвариант.
Полностью нагружены освобождение `scopeKey` (21 тест на одну мутацию),
регистрация у родителя (12), однократность `unmountScope` (18),
`_closeCompleter` (26), таймер `Zone.root` (16).

**Перемерено 2026-08-31 шестым ревью, и две мутации выжили на всей сьюте** —
обе стали находками Fable (M1 и одна из Low). Главная: **отказной канал разбора
для зависимости чужой реализации не держался ничем** — обе точки отправки
удалялись целиком, и сьюта оставалась зелёной. Это то же место, куда с другой
стороны пришёл Codex своей M1. Разбор — в `2026-08-31[3]-project-review.md`.
**Закрыто 2026-08-31, `f377aea`:** пять тестов, и каждая из шести веток
проверена своей мутацией — обе точки отправки, охрана вокруг корня, имя в
отказе и охрана вокруг каждой ветви конкурентной группы.

**Перемерено ещё раз 2026-09-09, послеволновым ревью, и это самый плотный
замер здесь до сих пор: около сорока обратных мутаций тремя исполнителями.**
Главный результат — отрицательный и потому ценный: **ни один из двенадцати
тестов волны не пережил снятия правки, которую сторожит**, и ни одного
ослабленного утверждения (ноль удалённых строк в шести файлах). Пробелы нашлись
не в том, что тесты проверяют слабо, а в том, чего они не проверяют вовсе:
стек укрытой ошибки (`_bodyStackTrace`) и `key` ветви группы снимались
целиком, а сьюта оставалась зелёной. Оба закрыты — первый удалением поля,
второй тестом. Новый приём, которого здесь раньше не было: **проверка
достижимости** — на место новой охраны ставится `throw`, и зелёная сьюта
означает, что до этого места не доходит ни один тест. Так нашлись две мёртвые
ветки. Разбор — в `docs/records/2026-09-09[5]-post-wave-review.md`.

**Из пяти названных пробелов открытым остался один, и закрыть его нельзя:**
release-ветки, отделённые от debug ассертом (M5, M18), не проверяются
принципиально, потому что `flutter test` всегда с ассертами. Четыре остальные
закрыты волнами 18–25; чем именно — в архивной записи и в вердиктах находок
H12, M11–M13 четвёртого ревью.

Что стоит унести дальше из работы над этими пробелами:

- **у одной охраны бывает два входа, и покрыт может быть один.** Волна 18
  разводила лимиты вокруг двух половин разбора и написала тест на висящий
  `disposeStateAsync` — то есть на тот вход, о котором говорила находка H5.
  Вход через висящую инициализацию к той же охране приходит раньше, и его не
  было. Правило то же, что у соседних реализаций, только внутри одного метода:
  **закрыл охрану — перечисли, чем в неё входят**;
- **мутация переключателя `boundsDisposeScopeItself => false` у `Scope` сьютой
  не ловится, и это, похоже, эквивалентная мутация, а не пробел.** Переключатель
  не убирает внутренние границы, он добавляет внешнюю поверх них: наблюдаемая
  разница — лишний отчёт об истечении в случае, когда висят обе половины.
  Настоящий откат H5 — свести две внутренние границы в одну, и **это** оба
  теста ловят. Записано, чтобы следующий не тратил время на ту же мутацию.

**Мигающих тестов не нашлось.** Дисциплина с реальным временем выдержана.

**`notifyDependents` и `setState` — две половины, и это закреплено тестом**
(`lite_scope_test.dart`, группа «notifyDependents and setState are separate
halves», 2026-08-20). Владелец спросил, сказано ли в `README.md`, что
`setState` в скоупе «отключен». Не сказано — и было бы неверно: слова
`setState` нет в `lib/src/scope/` вовсе, состояние скоупа — обычный `State`.
Проба показала расклад: `notifyDependents()` доходит до подписчиков и
собственный `build` состояния **не** выполняет, `setState` — наоборот. Обе
половины теперь названы в `README.md` таблицей, а ловушка («поменял поле,
позвал только `notifyDependents()` — свой `build` не выполнится») названа
прямо. Тесты нагруженные: мутация `_rebuildIsNotifyOnly = false` валит первый,
добавленный `notifyDependents()` в `bumpWithSetState` — второй.

Темы поправлены следом, по просьбе владельца: в `doc/lite_scope.md` аналогия
(«тот же контракт, что у `StatefulWidget` с `setState`») заменена на прямое
изложение обеих половин, и по правилу «найди то же у соседа» то же дописано в
`doc/full_scope.md` — состояние `Scope` описывалось так же односторонне.
Попутно в `lite_scope.md` исправлено «four additions» над таблицей из пяти
строк (и «четырьмя добавками» в зеркале). `doc/scope_model.md:137` проверен и
оставлен: там `setState` у `StatefulWidget` **над** скоупом, случай другой и
описан верно. `doc/scope_widget.md` тоже оставлен: у `ScopeWidget` состояния
нет, и `setState` там ни при чём.

Раздел ведётся по факту. Прежняя формулировка «крупных пробелов не осталось,
покрыты все слои» была неверна и снята после повторного ревью. Волны 10–13
закрыли весь известный на тот момент список — **и третье ревью открыло новый**.

**Покрыто сильно:** освобождение `scopeKey`
(`test/async_scope_coordinator_test.dart`), порядок разбора на обоих путях
(`test/scope_lifecycle_order_test.dart`), `close()` во всех четырёх состояниях
и `ScreenshotReplacer` прямыми виджет-тестами (`test/lite_scope_test.dart`),
дерево из десяти зависимостей с четырнадцатью комбинациями падений
(`test/scope_auto_dependencies_test.dart`), `throwIfDisposed`
(`test/listenable_utils_test.dart`), пять путей разбора контроллера
(`test/async_controller_scope_test.dart`).

**Что осталось непокрытым сознательно:** `reportName` у чужого родителя,
статика `AsyncControllerScopeCore` (те же `ScopeContext.*`, покрытые у соседних
слоёв) и охрана кадра в `_currentBuildPass()` — у последней нет достижимого
сценария, и это записано в вердикте P3-4.

**Тест-пустышка остался один** — `test/async_scope_state_test.dart` целиком: оба
теста считают скобки в `AsyncScopeError.toString()` и пройдут при
`toString() => ''`. Судьба пустышек — решение владельца, поэтому не тронуты.
Список был длиннее на четыре записи, и все четыре к 2026-08-30 недействительны,
проверено чтением файлов: тавтологический ассерт в `test/scope_logger_test.dart`
и шесть тестов в цикле `example/navigation_node/test/lessons_test.dart` ушли
вместе со своим кодом (журнал заменён наблюдателем 2026-08-19, `NavigationNode`
вынесен 2026-08-24), а две оставшиеся **исправлены**: в
`test/async_controller_scope_test.dart` тест «every caller of performDispose
sees the same failure» теперь и правда сравнивает через `same(...)`, а в
`example/scopo_demo/test/async_controller_example3_test.dart` обе позиции
проверяются на `isNonNegative` до сравнения, и над ними стоит комментарий,
объясняющий, чем тавтология была опасна. Прежний список — в архивной записи.

Что осознанно не покрыто и покрыто быть не может — гарантии анализатора:
закрытость `State.dispose`, закрытые обёртки `ScopeController`, запечатанные
волной 11 `initScope`/`initDataAsync`/`disposeScope`, и не-nullable `value` у
`.value`-конструкторов (там гарантия компилятора). Сюда же — снятая волной 12
аннотация `@visibleForTesting` с `LiteScopeCoreState.widget`: сам отказ теперь
проверяется тестом, а вот то, что геттер запечатан, — нет.

## Что дальше

**Очередь работ пуста.** Ревью волны правок 2026-09-09 проведено и
исчерпано — списки `docs/records/2026-09-09[5]-post-wave-review.md` и
`docs/records/2026-09-09[6]-post-wave-review.md`, у каждой из 22 находок
вердикт. Списки ревью до него —
`docs/records/2026-09-09[3]-bug-hunter-review.md` и
`docs/records/2026-09-09[4]-bug-hunter-review.md` — тоже пройдены целиком.
**0.14.0 выложена на pub.dev 2026-09-09**, тег `v0.14.0` на `b0c3830`.

**Выкладка 0.14.0 на pub.dev проверена 2026-09-11, всё на месте.** `latest`
= 0.14.0 (опубликована 2026-09-09T13:17:43Z), `api/documentation/scopo` даёт
`completed`, страница пакета и индекс `documentation/scopo/0.14.0/` отдают 200
без редиректа на предыдущую версию, pub points 160/160. Проверено именами,
которых в 0.13.0 не было: `scopo/ScopeInitContext-class.html` и
`scopo/ScopeInitJob-class.html` — 200; ушедшие `ScopeInitCancelled` и
`ScopeInitHandle` — 404.

**Поправка к рецепту проверки, иначе 404 примут за невыложенную
документацию:** `ScopeInitContext` и `ScopeInitJob` — **классы, а не темы**, и
по адресу `topics/<Имя>-topic.html` они отдают 404 всегда, выложена
документация или нет. Тем у пакета одиннадцать, и все одиннадцать были и в
0.13.0, то есть темой новизну 0.14.0 проверить нечем — проверяют
`<библиотека>/<Имя>-class.html`. Попутно видно, что **документация 0.13.0
стоит `failed`**: на дальнейшее не влияет, `latest` собралась, но объясняет,
почему тем «которых не было в предыдущей версии» искать бессмысленно.

Работы по находкам не осталось. Что стоит помнить из этой волны: **находка,
полученная чтением, — заявка, но заявка хорошая**: три из трёх подтвердились
зондами дословно, а одна (L3) так же дословно не подтвердилась, и различить
их можно было только прогоном. **Зонд находит соседа**: проверка M4 обнажила
двадцать мест того же сорта, которых не видел ни один из четырёх проходов.

**Заявка потребителя от 2026-09-07 сделана** (`3b11872`), и вместе с ней в
0.14.0 легли две ломающие правки. Что по ней осталось знать:

- **третья просьба закрыта наполовину и намеренно** — вердикт в §4 спеки; как
  пойдёт дальше, покажет потребитель, когда возьмёт эту версию;
- **форма A из разбора** (хуки скоупа принимают ошибку, а отчёт делает их
  реализация по умолчанию) не сделана и не отброшена: она не противоречит
  сделанному и может лечь сверху, если политика «на приложение» окажется
  недостаточной;
- **0.13.1 не выпускаем** — решение владельца от 2026-09-07, разбор в
  «Решениях владельца, которые действуют дальше». Ждать больше нечего:
  0.14.0 на pub.dev, потребитель берёт её оттуда.

**Переход на ядро сделан** (`bc914c8`, разбор — в «С чего начать», спека
`docs/records/2026-09-09[1]-async-job-migration-design.md`); находки ревью
того же дня закрыты все. **Глубже переход не пойдёт:** вопрос «переводить ли
на задачи и сами зависимости» разобран и закрыт отрицательно —
`docs/records/2026-09-09[7]-dependency-job-design.md` и две проверки на неё,
`docs/records/2026-09-09[8]-dependency-job-report.md` и
`docs/records/2026-09-09[9]-context-reach-report.md`.
Ядро опубликовано как **`async_job` 0.1.0** — имена `job` и `jobs` сервер не
принял, разбор в `solo`,
`docs/records/2026-09-05[8]-job-name-request-report.md`. Остаток спеки
`docs/records/2026-09-05[1]-init-wait-family-design.md` закрыт вместе с
переходом: `join` взят, потому что пришёл с ядром.

**Что осталось решить владельцу — две вещи, и обе несрочные.** Публикация
0.14.0 и порядок правок по ревью закрыты 2026-09-09.

- **вариант C по `initCancellationTimeout`** («пережить истечение»): ядро
  сделало его дешевле — значение и его диспозер живут на стеке задачи, а не
  в элементе, — но `canReleaseAfterCancellation` ядро за нас не решает,
  релиз по-прежнему колбэк виджета. Отдельная работа, спека Р5;
- **нужен ли нам `JobStream.each`** ядра. Стрима в инициализации нет, но
  `ScopeModel` мог бы им пользоваться. Вопрос открыт в конце спеки перехода.

**Сверено с соседом 2026-09-09.** В `~/development/my/solo` опубликовано
**только ядро**: `packages/solo` 0.2.0 и `packages/flutter_solo` 0.2.0 готовы,
но ещё не выложены. **Имя может переехать:** если `job` освободится, тот же
код выйдет как `job` 0.1.0, `async_job` пометят discontinued, и scopo
поменяет строку импорта и `pubspec.yaml`.

Читано у ядра: публичные интерфейсы (`Job`, `JobContext`, `Outcome`,
`JobObserver`) и разложение исхода в `job_base.dart:756`–`:860`; внутренности
стека уборки и детей приняты по дартдоку, а не по коду.

**Выпуск 0.14.0 прошёл 2026-09-09 и прошёл не по протоколу** — это главное,
что из него стоит унести. Связка была разорвана: сперва `dart pub publish`, а
тег и `push` через пятнадцать минут отдельной просьбой, так что версия
какое-то время стояла на pub.dev без тега. Отсюда правило владельца, записанное
в `AGENTS.md` §5, подраздел «Выпуск версии»: коммит `Release X.Y.Z`, тег
`vX.Y.Z`, `push`, `dart pub publish` — целиком, подряд и только по явной
просьбе, состоящей ровно из просьбы опубликовать. Ещё одна мелочь оттуда же,
стоившая лишнего шага: **теги здесь лёгкие, и `--follow-tags` их не
отправляет** — нужен явный `git push origin vX.Y.Z`.

**Все очереди ревью пусты.** Ниже — про списки 2026-08-31, они исчерпаны
давно. Оба списка
послеволнового ревью 2026-08-31
(`2026-08-31[4]-post-wave-review.md` — Fable, 4 находки;
`2026-08-31[5]-post-wave-review.md` — Codex, 6) исчерпаны
2026-09-01: у каждой вердикт в своём файле. **Волну этих закрытий проверили
третьим и четвёртым проходом** (`2026-09-01[1]-post-wave-review.md` — Fable;
`2026-09-01[2]-post-wave-review.md` — Antigravity на Opus 4.6): одна находка
Info на двоих, закрыта `706b837`.
Третий проход подряд по одному коду и первый почти пустой — на этом цепочка
«ревью → волна → ревью волны» и останавливается. Все 29 находок шестого ревью и
списки шести предыдущих закрыты; чем и когда — в вердиктах самих ревью и в
`docs/records/2026-08-30[1]-handoff-history-log.md`. `docs/backlog.md` пуст.

**Прежние решения закрыты.** Публикация 0.13.0
состоялась 2026-09-01 по явной просьбе владельца: тридцать пунктов, четыре ломающих,
тег `v0.13.0` на `f0abf0e`. Протокол публикации прошёл четвёртый раз без
отклонений, и всё, что в нём записано, подтвердилось; единственное, чего в нём
нет и что стоит добавить при случае, — проверка выкладки документации темой,
которой не было в предыдущей версии, здесь не сработала: новых тем 0.13.0 не
принесла.

**Два других решения закрыты 2026-08-31** и живут в «Решениях владельца…».
`disposeScope` после упавшего `initScope`: после сбоя нужна частичная
утилизация, и какая именно — знает только `initScope`; `disposeScope` делает
полную и от этого выяснения свободен, а правило соседнего этажа
(`disposeStateAsync` выполняется и после упавшего инициализатора) сюда не
переносится. И сьюта примера едет в архив намеренно — причина в `.pubignore`.

**Если работа появится** — порядок обычный: `AGENTS.md` §8 (сперва падающий
тест, потом правка, потом проверка нагруженности откатом; закрыл — ищи то же у
соседа) и §4 (у каждой проработанной находки ревью — вердикт абзацем в конце
неё, в самом файле ревью). **Работа не из списка ревью начинается с записи
дизайна** в `docs/records/` и с согласия владельца: решения о публичной
поверхности принимает он, а не агент. Так шли две последние —
`2026-08-28[1]-step-entry-notification-design.md` и
`2026-08-29[2]-disposal-exit-channel-design.md`.

**Соседа ищи мутацией, а не глазами.** Шесть P1 третьего ревью были
пропущенными соседями закрытых находок, и все шесть нашлись запуском кода, а не
чтением. Результат поиска пишется в отчёт волны.

**Порядок публикации, чтобы не выяснять заново** (прошёл пять раз — 0.10.0,
0.11.0, 0.12.0, 0.13.0, 0.14.0). **Разрешение владельца и границы связки — в
`AGENTS.md` §5, «Выпуск версии»; здесь только механика.**

Подготовка — свободно, это откатываемые правки в дереве:

1. поднять `version:` в `pubspec.yaml` и **тем же коммитом** пересоздать локи
   обоих примеров закреплённым тулчейном — `git diff example/*/pubspec.lock`
   должен показывать ровно `version:` пакета;
2. заполнить раздел `CHANGELOG.md`;
3. гейт §6 целиком → `git status` чист → CI зелёная на голове;
4. `fvm dart pub publish --dry-run` на **чистом** дереве (0 warnings; на грязном
   он считает незакоммиченную правку предупреждением, а предупреждение — это
   выход 65).

Дальше — связка, целиком и подряд, по отдельной явной просьбе владельца:

5. коммит `Release X.Y.Z`;
6. **lightweight-тег `vX.Y.Z`** на ту же голову;
7. `git push origin main` **и отдельно** `git push origin vX.Y.Z` —
   **`--follow-tags` лёгкий тег не отправляет**, он берёт только
   аннотированные; проверять `git ls-remote --tags origin`;
8. `fvm dart pub publish --force` — **`--force` здесь не «мимо проверок», а
   «без интерактивного вопроса»**: обычный `publish` спрашивает `y/N` и в
   неинтерактивной оболочке повисает, валидацию он всё равно проходит целиком.

Авторизация лежит в `~/Library/Application Support/dart/pub-credentials.json` —
**именно там, на macOS, а не в `~/.config/dart/`**; токенов `pub token list`
для pub.dev не нужно, это про сторонние репозитории. **`dart pub logout` этот
файл удаляет**, и повторный вход интерактивен — открывает браузер, из
инструмента агента не проходит. На 0.14.0 я стёр учётные данные владельца
именно так: посмотрел не в тот каталог, решил, что входа нет, и позвал
`logout`, чтобы «проверить состояние». Проверка состояния здесь — `ls` по пути
выше, и ничего больше.
Индекс pub.dev обновляется **до** десяти минут: это верхняя граница, а не
гарантия задержки — дважды из трёх он отдавал новую версию сразу.

Отдельная грабля публикации: **страница пакета на pub.dev показывает `README.md`
из архива опубликованной версии**, а не из репозитория, и **документация
выкладывается не сразу**. Пока документация новой версии не выложена, pub.dev
редиректит любой её URL на предыдущую версию, а `api/documentation/scopo` при
этом уже говорит `completed`. Проверять надо **именем, которого в предыдущей
версии не было**: 404 — ещё старая, 200 — выложена. **Имя берут по тому
адресу, по которому оно живёт:** тема — `topics/<Имя>-topic.html`, класс —
`<библиотека>/<Имя>-class.html`; класс, запрошенный как тема, отдаёт 404 и
без всякого редиректа. Новых тем версия может не принести вовсе — так было и у
0.13.0, и у 0.14.0, — тогда проверяют новым классом.

## Как здесь работают

Полный регламент — в `AGENTS.md`. Коротко: тест до исправления и проверка
нагруженности откатом (мутацией); `docs/handoff.md` обновляется по ходу, а не в
конце; у каждой проработанной находки ревью — вердикт абзацем в конце неё, в
самом файле ревью; выполненное удаляется из `docs/backlog.md`; мерж прямо в
`main`; чужие правки в коммит не попадают; правка оригинала в `doc/` не заканчивается без
правки зеркала в `docs/ru/` и `sh docs/ru/stamp.sh`; перед мержем прогоняются
восемь команд из раздела 6 `AGENTS.md` (через `fvm`, тулчейн 3.27.0).

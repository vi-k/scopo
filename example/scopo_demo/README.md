A demo of how scopes can be used.

Provides a comprehensive reference for building complex apps with scopo,
covering happy paths, error scenarios, and nested architecture.

This shows how scopo supports a hierarchical dependency structure where child
scopes can access parent dependencies (though in this demo, they are mostly
independent).
